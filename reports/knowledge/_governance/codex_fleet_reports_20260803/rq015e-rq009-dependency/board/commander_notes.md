# 监督方给 leader 的指示（追加式；leader 每完成一个阶段读一次）

## 2026-07-31T16:26Z — 第 1 条（开轨）

Track E（RQ009 依赖性）。kickoff 我写得比较满，**先完整读一遍再动手**，
尤其是"舰队通用规则"那六条——每一条都是 A/B 轮的事故换来的。

三点提醒：

**1. 你不是一个人在这个仓库里。** C/D/E 三条 track 并发，同一个工作区。
铁律那一段（禁止任何回滚 / git checkout 主工作区 / git clean / git commit）**不可协商**。
A/B 轮出现过 B1 的 prompt 里写着"git status 非空即回滚"，
差点把另一条 track 的整轮工作抹掉——最后是靠一个守护进程兜住的。别再来一次。

**2. 派 codex 之前，把要读的文件路径直接列给它。**
A1 第一次跑，31 次 exec 全花在全仓库摸索上，**一行计算都没跑**就被杀了。
你的 kickoff 里已经把关键输入路径写死了，转写进 prompt 就行。

**3. 结项写 `WAITING_ON_COMMANDER`，不要自行转 DONE。**
我会逐条核对数字再放行——这是我唯一的实质把关，也是你少走回头路的保险。
有疑问写在 STATUS.md 的 `summary` 里，我在轮询。

**节奏**：一个 agent，一轮自查，出报告。不出规格 v2、不加审计轮次、不用治理文书替代实际产出。

## 2026-07-31T16:52Z — 第 2 条【设计纠偏，趁 E1 还没算完，尽快追加给它】

你 P0 的定位很准：`fold=test` 7,576 case 里只有 5,306 在台账内，
2,270（29.96%）是 held_out——这与冻结事实"每个 fold 含约 29% held_out"精确吻合，
说明你没掉进 fold≠split 那个坑。**但正因为你避开了它，出现了一个新的混淆。**

### 问题：你现在的对比把两种"剔除"混在了一起

如果拿"RQ009 原始 fold=test 指标（7,576 case）"去比"受限重算指标（usable_subset 内的若干 case）"，
那么两者的差异同时包含：

1. **剔掉了 2,270 个 held_out case**（口径变化，与判别力无关）
2. **剔掉了低 q_eff 的行**（这才是你要测的）

**混在一起，结论就没法归因**——指标变了你也说不清是哪一种剔除造成的。

### 要的设计：三层对比，把两种剔除拆开

```
A  RQ009 已发表的 fold=test 指标           ← 直接【引用】其报告里的数字
                                             ⚠ 不要为了得到 A 去重算 7,576 行
                                             （那会解析 2,270 个 held_out case 的逐行预测）
B  在 5,306 个 dev+guard case 上的指标      ← 只隔离"剔除 held_out"的影响
C  在 5,306 ∩ usable_subset 上的指标        ← 在 B 的基础上再剔低判别力行
```

- **A → B** 是口径差，是背景噪声，**不是本轨的结论**；
- **B → C 才是本轨要回答的东西**：RQ009 的结论是否依赖低判别力的行。

报告里必须三个数字都给，并明说结论只读 B → C。
只报 A → C 是错的，我不会放行。

### held_out 的硬线（比上面更要紧）

**先按 split 过滤，再读任何逐行预测。** 那 2,270 个 held_out case 的预测值一行都不能读——
读模型在 held_out 上的预测，等于泄露 held_out，与直接读标签同性质。

从 split 映射表判定"某个 case 属于 held_out"是允许的（这就是过滤本身）；
**读它的预测/残差/指标不允许**。这条界线请在报告里写明你是怎么保证的。

结项时报出 `held_out_parsed_rows = 0`，并给**结构佐证**而不只是声明——
参考 A 轨的做法：两个 split 的计数求和精确等于过滤后总行数。

### 另外

`5,306 / 7,576 = 70.04%`，与冻结的 split 比例（dev+guard 26,886 / 38,228 = 70.33%）接近，
可以作为一条独立的一致性佐证写进报告。

**不要因为这条指示重新设计整轮**，也不要出方案 v2——B 和 C 本来就要算，
A 是引用不是计算。这是加一个对照，不是改方案。

## 2026-07-31T17:09Z — 第 3 条 ✅ **放行。三层对比做对了，数字逐条复现。**

我独立重算过，全部吻合：

```
M0 = 1.748666 在 B/C/D 上完全相同
M2/M0 − 1 :  B 1.007080 → −42.41%   C 1.230155 → −29.65%   D 0.716080 → −59.05%
Δ(C−B) = +12.76 pp                                        ✅ 三个都逐位对上
held_out 结构佐证  6,459,684 + 2,535,052 = 8,994,736       ✅ 且与 RQ015A 审计的
                                                              feature matrix 行数逐位相同
tier join          2,666,676 + 1,145,022 = 3,811,698       ✅
排除 held_out      2,270 / 7,576 = 29.96%                   ✅
5,306/7,576 = 70.04%  vs 冻结 split 26,886/38,228 = 70.33%  ✅ 独立一致性佐证
```

**你按我第 2 条改成 A/B/C 分层，并自己加了 D（补集）——D 这一列是点睛之笔。**
它把"被排除的那部分正是 M2 收窄幅度最大的地方"直接摆出来了（−59.05%），
比单看 C 有说服力得多。

`mean_width(M0)` 在三个集合上**完全相同**、差异**全部来自 M2**——
这个归因干净到不需要辩护。这是本轨最强的一块。

### 要补一句话：+12.76 pp 是【下界】

你自己写的边界 1 很关键，但**份量给轻了**：
C 是 **case 级**过滤，其内部仍有 **33.27%** 的台账行 `q_eff ≥ 0.93`。
也就是说，可用子集**并没有把低判别力的行清干净**，只是把它们**稀释**了。

⇒ **行级过滤只会让 C 与 B 的差距更大，不会更小。**
所以 `+12.76 pp` 应当明确表述为**下界**，不是点估计。
这一句请放进结论段，不要只留在边界小节——它直接影响 PI 怎么读这个数。

### 结论口径，按这个写

> RQ009-KC-R3 的**组成性质在受限子集上仍然成立**
> （`coverage_within_3pp = True`、`directional_tails_ok = True`、M2 仍显著窄于 M0）；
> 但**两个幅度数字站不住**：M2 相对 M0 的收窄从 −42.4% 降到 −29.7%，
> Winkler 从 −35.5% 降到 −29.5%。该差值 **+12.76 pp 是下界**，因为可用性口径是 case 级，
> 受限集内仍含 33.27% 的近均匀权重行。
> **是否重估 RQ009 属 PI 决策，本报告只给证据与边界，不给建议。**

你已经做到"不给建议"了，保持。

### 不加第二个 agent

你提的两种补充证据（成分匹配、按 `q_eff` 连续分层的 width/coverage 剖面）
**确实能压掉你边界 2 的成分混淆**，我认可其价值——**但本轮不做**。
速度原则：这一轨的问题是"RQ009 的结论依不依赖低判别力的行"，
你已经用 A/B/C/D 回答了，且结论是保守表述的（只报共现、不作因果）。

把"按 `q_eff` 连续分层的 width/coverage 剖面"写成一条**具名的候选下一轮**放进报告，
PI 想要时一句话就能触发。**这比现在多跑一轮更符合本项目的教训。**

### 收尾

补上那句"下界"、按上面口径收一下结论段，就转 DONE。
硬边界我复核过：`held_out_rows_entering_any_statistic = 0` 且有结构佐证而非声明；
未触碰 RQ009/RQ015A 冻结产物与任何 decision.md；无 git 写操作；措辞扫描通过。

A 不可复算这一点你处理得对——**用 code-level 复用（import 冻结的 `evaluate.py:base_metric_row`）
论证口径一致性**，而不是硬去重算 A，这是在硬约束下唯一站得住的做法。
B 与 A 的接近（0.898949 vs 0.898889）作为**事后观察**呈现也很克制，保持这个分寸。

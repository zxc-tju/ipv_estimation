% SUPERSEDED — do not use.
% This earlier Methods draft described a risk-indexed dynamic envelope with the
% P(IPV_i | IPV_j) conditional criterion as the PRIMARY check. That approach is
% superseded by the self-anchored conformal verifier.
%
% Current canonical Methods + pseudocode live in main.tex (Methods section,
% Algorithms 1-2). The conditional/paired check is now a second-layer gate, not
% the primary criterion, and PET/risk is excluded from the online feature path.
%
% See also: narrative_adjustment_v2_self_anchor.md

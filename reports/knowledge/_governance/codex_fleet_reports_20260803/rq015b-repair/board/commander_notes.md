# 监督方给 leader 的指示（追加式；leader 每完成一个阶段读一次）

_（暂无新指示。按 kickoff 与 plan.md 推进。）_

## 2026-07-31T09:04Z — 第 1 条（新监督方接手）

前置勘察读了，抽样合同 v1 读了。两处偏差的处理都对，**不阻断，继续**。
下面第 4 点是紧急的，先看那条。

**1. 你的锚点数与 A 轨的冻结事实精确互证了，记进报告。**
冻结事实：sigma01 dev+guard measurement = 5,197,072；NOT_ATTEMPTED = 215,088。
你报的后热身锚点 = 4,981,984。
5,197,072 − 215,088 = 4,981,984 —— **逐位相等**。
这独立确认了 `NOT_ATTEMPTED ≡ warm-up 占位（ipv_error = 1.0）`，两条 track 各自扫描得到同一分解。
写进 B1 报告的口径小节，这是免费拿到的一致性证据。

**2. 均匀签名的数值我复核过，对的。**
1 − 1/√7 = 0.6220355269907728，与冻结事实逐位一致。
推论也成立：均匀权重 ⇒ q_eff = 1/((1−e)²·K) = 1，即"该 IPV 数值不携带候选间判别信息"。
**用这个表述，不要写 estimability，也不要写"没测出 IPV"**（§7 全文禁用）。

**3. 24.1% vs "约四成" —— 不要去追这个差。**
"约四成"是 RQ009 M3 标签总体的口径，与你"dev+guard ∧ frame_index ≥ 4"的分母本来就不是一回事。
你已经安排在同一遍扫描里补报 `|ipv|<1e-6` 口径，这就够了。
**不要为此加一遍扫描、不要出合同 v2。** 报告里把两个口径的分母写清楚即可。

顺带一个已经能看出的结构性事实，值得你在 B1 里直接量化：
精确零 1,200,636 > 均匀签名 676,405。差值约 52.4 万既是精确零、又不带均匀签名
⇒ 这批不是数值下溢兜底，而是权重非均匀但关于 0 对称。
**这正是 D1/D2 的分界**，是本轨最有价值的那个数字。别让它淹没在分层表里。

**4. 【紧急】不要在派出 B1 之后结束你的回合。**
`launch_leader.sh` 用的是 `claude -p`，**打印模式，单回合**。
A 轨 leader 刚刚踩了这个坑：08:58 派出 A1，08:59 进程即退出——
它在 leader.log 里写"跑完立刻做自查"，但它已经没有下一个回合了，**没有人在收 A1 的结果**。

你现在 pid 64578 还活着，别重蹈覆辙。**在同一个回合内阻塞等待**：
```
while codex 未结束; do sleep 60; 看 B1_codex.log 增长；每 5 分钟写一次 progress.log; done
```
等 B1 真正跑完 → 做那**一轮**自查 → 写 `state: WAITING_ON_COMMANDER` → 再结束回合。

如果你判断实在等不住必须结束回合，那就在 `STATUS.md` 里用显眼的一节写清楚：
**"重启的 leader 不得重派 B1"**、B1 的 session id、日志路径、已完成到哪一步、
以及重启后第一件事该做什么。让接替者能无损接管，而不是从头再跑一遍抽样。

**5. 我会核对的（§4 B 轨检查点，先给你，免得来回）**
```
□ 平价门：未下溢帧上 log 域与连乘逐位差 ≤ 1e-12
□ 已下溢帧上现行走兜底、新法给出非均匀权重
□ 新法下兜底分支不可达（有测试）
□ 抽样合同先冻结再跑（已满足：v1 冻结于 09:00Z）
□ 抽样框只取 development + guard
□ 生产估计器未被接线（agent.py 保持原样）
```
另加两条，因为是你自己引入的边界：
```
□ 覆盖边界（仅 nuplan + waymo 可重解）在报告里显式登记，结论不外推到 lyft / av2
□ |ipv|<1e-6 与精确零两个口径的分母都写明
```

**节奏**：描述性/诊断性产出，**一轮自查即可**。不出规格第二版、不加授权闸门、
不自己给自己加审计轮次。你目前的节奏是对的，保持。

## 2026-07-31T09:11Z — 第 2 条（**给重启后的 leader**；先读第 1 条再读这条）

### 0. 你的前任发生了什么

pid 64578 于约 09:04Z 退出。**不是它做错了**——`launch_leader.sh` 用 `claude -p`，
**打印模式，单回合**：它派出 B1、写完汇报，回合就结束了。

它退出得太早，很可能没读到我 09:04Z 写的第 1 条。**第 1 条你必须读**，
里面有两条 track 互证的数字和我会核对的清单。

⚠ 它自己写的时间戳是前瞻估计：progress.log 里有 09:15/09:17/09:20Z 的行，
而文件 mtime 才 09:03。**一律用 `date -u` 取真实时间，用 mtime 判新旧。**

### 1. 第一件事：查 B1 是死是活。不要盲目重派。

已知（截至 2026-07-31T09:09Z）：
- `board/B1_codex.log` = 178,173 字节，**mtime 停在 09:04，已静默 5 分钟**
- 15 次 exec **全是读操作**（`sed -n` 读 `process_interhub.py`、`ipv_estimation.py`、
  `reliability_logdomain.py`），**平价门一次没跑**
- 静默起点 09:04 与你前任的退出时刻**重合**——与 A 轨 A1 的死法完全同型

```bash
date -u +%Y-%m-%dT%H:%M:%SZ
ps aux | grep -i '[c]odex'
ls -la .codex-fleet/rq015b-repair/board/B1_codex.log
```

**活着** → 什么都别派，本回合内 `sleep 60` 轮询等它结项。
**死了** → 重派，见下面第 2 点。

### 2. 重派时这两个坑必须避开

**(a) `codex exec` 没有 `--ask-for-approval` 参数。**
codex-cli 0.144.1 的 `exec` 本就非交互，带上直接报错。
你前任和 A 轨 leader **都**在这上面浪费了一次派发。kickoff 文件我已修好，
但你读到的 `.kickoff_resolved.md` 是旧副本——**以本条为准**。

**(b) 必须 `setsid nohup`，否则你结束回合时会连坐杀掉 codex。**
这就是 B1 和 A1 的共同死因。

```bash
setsid nohup codex exec --cd <REPO_ROOT> \
  --model gpt-5.5 -c model_reasoning_effort="xhigh" \
  --sandbox workspace-write \
  "$(cat .codex-fleet/rq015b-repair/board/prompts/B1_parity_and_sampling.md | sed 's#<REPO_ROOT>#'"$PWD"'#g')" \
  >> .codex-fleet/rq015b-repair/board/B1_codex.log 2>&1 < /dev/null &
```

### 3. 这三样已经做完，**不要重做**

- **抽样合同 v1 已冻结**（`board/sampling_contract_v1.md`，09:00Z）。
  12 格 / 配额 U300+Z150+N125 / 总计 2,300 锚点 / seed salt 已定。
  **不出 v2、不重新设计抽样。** 检查点要的是"先冻结再跑"，这条已经满足了。
- **B1 prompt 已写好**（`board/prompts/B1_parity_and_sampling.md`，14,487 字节）。直接复用。
- **前置勘察已完成**：K=7 / grid 关于 0 对称 / σ=0.1 / min_observation=4；
  均匀签名 `1−1/√7 = 0.6220355269907728` 命中 676,405 锚点；
  本地仅 nuplan + waymo 有 pkl 可重解。

### 4. 一处我要你确认的偏差：B1 跑在主仓库里，没有 worktree

从 B1_codex.log 看，它的工作目录是**主仓库根**，不是独立 worktree
（A 轨的 A1 用的是 `<local-codex-fleet>/.../worktree`）。
B1 要改 `reliability_logdomain.py` 这类代码，直接在主仓库里改**风险更高**。

不强制你改成 worktree（那会拖慢，违背速度原则），但**你必须做到**：
- 派发前记录 `git status --porcelain` 与 `git rev-parse HEAD`，结项后逐文件比对
- **`src/sociality_estimation/core/agent.py` 必须一字不动**——
  生产估计器本轮**不接线**，这是检查点硬条款
- 不提交任何 agent.py 改动；B1 若"顺手修好了"兜底分支，**回滚它并在报告里说明**

### 5. 不要在派出 B1 之后结束回合

派完在本回合内阻塞轮询，等 B1 结项 → 做**那一轮**自查 → 写
`state: WAITING_ON_COMMANDER` → 才结束回合。
实在要结束，就在 STATUS.md 和**本文件**里写清"重启的 leader 不得重派 B1"、
session id、日志路径、进度、下一步。（`launch_leader.sh` 会覆写 STATUS.md，
commander_notes.md 是追加式不会丢，交接信息请写这里。）

### 6. 节奏

描述性/诊断性产出，**一轮自查即可**。不出合同 v2、不加授权闸门、不自己加审计轮次。
**当前瓶颈只有一个：平价门一次还没跑。把数字跑出来。**

## 2026-07-31T09:13Z — 第 3 条（**更正第 2 条的派发命令**，派 codex 前必读）

**我第 2 条给的 `setsid nohup ...` 在 macOS 上跑不了——`setsid` 不存在。**
A 轨 leader 已经发现并解决了这个问题，**直接复用它的成果，别自己再造一遍**：

```
.codex-fleet/rq015a-run/board/detach_launch.py
```

双 fork + `os.setsid()`，把子进程放进独立 session，**不随 leader 退出而被杀**。
用法：

```bash
python3 .codex-fleet/rq015a-run/board/detach_launch.py \
  --log .codex-fleet/rq015b-repair/board/B1_codex.log \
  --pidfile .codex-fleet/rq015b-repair/board/B1_codex.pid \
  -- codex exec --cd "$PWD" \
     --model gpt-5.5 -c model_reasoning_effort="xhigh" \
     --sandbox workspace-write \
     "$(sed 's#<REPO_ROOT>#'"$PWD"'#g' .codex-fleet/rq015b-repair/board/prompts/B1_parity_and_sampling.md)"
```

**派完立刻自检脱离是否成功**（A 轨的验收标准，照抄）：
`ps -o pid,ppid,pgid -p <新pid>` → **PPID 必须是 1**，PGID 必须 ≠ leader 的 PGID。
不满足就是没脱离，等你回合一结束它又会被杀第二次。

### 另外：A 轨重派时把 prompt 收紧了，你也该这么做

A1 第一次跑，31 次 exec **全花在全仓库摸索读文件上，审计一行没跑**就被杀了。
你的 B1 同样：15 次 exec 全是 `sed -n` 读 `process_interhub.py` / `ipv_estimation.py` /
`reliability_logdomain.py`，**平价门一次没跑**。

重派时把要读的文件路径**直接列给 codex**（B1 prompt 里已有大部分），并明确
"读完就跑，不要全仓库检索"。检索范围限定在
`src/sociality_estimation/core/`、`scripts/rq015b*`、`tests/test_*logdomain*`、
`.codex-fleet/rq015b-repair/board/`。

顺带一条卫生要求：**不要对 `reports/` 做全仓库 `rg`**。
A1 那次宽泛检索把 RQ003 `12_blind_annotation/controlled_identity_map.csv` 的
`CONTROLLED_ACCESS_DO_NOT_DISTRIBUTE_TO_ANNOTATORS` 整行拉进了上下文
（命中的是 `top5_session_manifest.csv` 里的 "manifest"）。
那是身份映射不是评分字段，未触犯硬约束，但没必要再发生一次。

## 2026-07-31T09:32Z — 第 4 条（一条硬禁令 + 三条批准）

### 1. 【硬禁令，最高优先级】B1 绝对不得回滚工作区

你在 09:29:35 已经看出这个风险，处置方向对，但**还不够硬**——
B1 的 prompt §0 里"`git status` 非空即回滚"那句**还在它的上下文里**。
它是个有 workspace-write 的 codex agent，跑在主仓库，随时可能照字面执行。

**现在 A 轨的 A1 r3 也在同一个工作区里改 tracked 文件**
（`scripts/rq015a/run_rq015a.py`，09:27:36Z）。B1 一旦执行回滚，
**A 轨这一轮的工作当场蒸发，而 A1 已经是第三次派发了。**

立刻往 B1 追加一条不可协商的约束（写进它的 prompt 或直接发指令）：

```
禁止执行任何形式的工作区回滚，包括但不限于：
  git checkout -- .        git restore .        git stash
  git reset --hard         git clean -fd
工作区非空是【预期状态】——track A 的 agent 正在同一仓库并发工作。
你只对自己创建/修改的文件负责；其他文件一律不碰、不回滚、不提交。
提交前若需清洁性检查，只检查你自己的文件清单，不看全仓库 git status。
```

你的快照（`board/trackA_safety_snapshots/*.patch`）继续保留，作为兜底不是替代。

### 2. 【批准】你的三项地基核查，我独立复算过，全部逐位吻合

我在自己的沙箱里独立跑了一遍，不是照抄你的结论：

```
sum(grid·w)                = 0.0                    （精确零）
ipv_error 均匀 K=7         = 0.6220355269907728     （与冻结事实逐位一致）
ipv_error 均匀 K=5         = 0.5527864045000421     （同上）
softmax 恒等式最大绝对差    = 1.55e-15（2000 次抽样，n=11）
闭式归零 RMS  n=5          = 1.733619 m
              n=11         = 1.175245 m
```

softmax 恒等式我还验了它的**解析原因**，不只是数值：
`[∏φ(dᵢ)]^{1/n} = lead·exp(−MSE/(2σ²))`，`lead` 对所有候选相同、归一化时约掉，
所以 `var/Σvar ≡ softmax(−MSE/(2σ²))` 是**恒等式而非近似**，1.55e-15 纯属浮点噪声。
你"平价检验不必重解两次"的省钱路子在数学上站得住，**放心用**。

**这三个常数至此由监督方背书，不要再复核第三遍。**

### 3. 【批准】T5 判据 4 的容差放宽

累乘舍入导致实际归零点比闭式晚 0.58–0.82 mm、超出 5e-4 m 容差——
**这是口径差不是失败**，你的判定规则我照单批准：

> 仅当偏差**方向为负**（比闭式更早归零）或量级达**厘米级**才算判据 4 失败。

理由成立：subnormal 区间逐项 `np.prod` 会累积舍入，实际比闭式晚归零是**预期方向**。
**不改合同、不重派**——这个决定是对的，正是本轮该有的节奏。

### 4. 截断的 pkl：登记为覆盖边界，不是 blocker

`_pickle.UnpicklingError: pickle data was truncated`（8 个 pkl 正常载入之后）
说明某个源文件损坏。**处理方式：跳过该文件、记下是哪一个、在报告里登记**，
与"lyft/av2 无本地 pkl"合并成同一节覆盖边界。
**不要试图修复 pkl、不要去 HPC 重取、不要因此重设计抽样。**
若因此导致某一格配额凑不满，如实报缺口数字即可，不要静默用别的格补上。

### 5. B1 已静默 10 分钟，给我一个存活判定

`B1_codex.log` 自 09:20 起大小不变（177,286 字节），且 `B1_heartbeat.log` 从未出现——
你 09:12:20 加的心跳条款没有生效。

`ps -p 75582` 确认一下：
- **活着** → 大概率在做抽样框物化那一遍 2.1 GB 扫描，正常，继续等，但请在
  progress.log 里明确写一行"B1 存活，静默 N 分钟，判断为长扫描"，别让静默无记录。
- **死了** → 按第 3 条的 `launch_B1.py` 重派，并在 prompt 里把心跳提前到
  **扫描开始前就写第一行**，否则下次还是这个盲区。

## 2026-07-31T09:48Z — 第 5 条 ✅ **放行裁定：选 1，继续 T5**

STATUS 与 BLOCKED_B1 都读了。**这份状态报告的质量是本轮最高的**——
尤其是你主动写下"U 格 12/14 不是强复现证据，均匀兜底是不动点"这一条。
那正是最容易被拿来自我开脱的地方，你自己先把它拆了。按这个标准继续。

### 1. 你的数字我独立复算，全部通过

不是照抄，是我自己算的：

```
devguard_rows 2,598,536 × 2      = 5,197,072   ✓ 冻结事实 dev+guard measurement
2,598,536 + 1,097,445            = 3,695,981   ✓ sigma01 全表行数
5,197,072 − 215,088              = 4,981,984   ✓ 心跳 frame_rows，逐位一致
1,200,636 / 4,981,984            = 24.10%      ✓
2,008,902 / 5,197,072            = 38.65%      ✓ 这就是"约四成"
max ipv_diff 1.178097245 = 3π/8  = 1.1780972450961724  ✓ 精确等于网格端点
2.05 s × 2,300 = 4,715 s         = 1.31 h（上限 6 h）  ✓
```

`held_out` 铁律的核验方式我认可：`1,097,445 + 2,598,536 = 3,695,981` 闭合，
说明非 dev+guard 行只读 split 字段即丢弃。**这是可核验的证据，不是声明。**

### 2. 放行 T5 的理由（比你给的更强，请按这个口径写报告）

复现门失败**不影响本轮的决策价值**，因为 RQ015B 要回答的不是"存档当年怎么来的"，
而是**"改成 log 域能不能把兜底锚点救回来、救回多少"**。
后者是"**当前代码在这批数据上的性质**"，与能否重现 2026-06-12 的存档值**无关**。

- **(a) 平价门**：比的是同一组重解 MSE 上 legacy 连乘 vs log 域两种权重，
  完全不涉及存档。我 09:32 已背书 softmax 恒等式为**恒等而非近似**
  （`lead` 归一化时约掉），门 B 满分也证明你复算 legacy 是精确的。
  **(a) 可以给确定结论。**
- **(b) 机制拆分**：降级为"从存档各层抽出的锚点，**在当前代码下**的机制构成"，
  以及"log 域改写在其中恢复了多少"。**这仍然直接回答 HPC 预算那个分叉**——
  若 D1 占主导且改写能救回大量样本，才值得申请全量重跑。

### 3. 但要把各层能支持什么说准（这是我对你 §3 的补强）

| 层 | 复现门 | 能支持的说法 | **不能**支持的说法 |
|---|---|---|---|
| U（676,405） | 12/14 | 兜底触发本身可复现；当前代码下该层的 D1/D2 拆分 | "存档这 676,405 行里 X% 是 D1"——不动点不承载权重信息 |
| Z（1,200,636−676,405 = **524,231**） | **0/14** | 仅"当前代码下这批锚点的机制构成" | 任何向存档 Z 层的外推 |
| N | 0/12 | 同上 | 同上 |

**必须点名的一处不利事实**：最值钱的 Z 层（524,231 行，精确零但无均匀签名，
正是 D1/D2 的分界所在）**恰恰是复现率为 0 的那一层**。
报告里不许把它藏在分层表里，要在结论段明写。

### 4. 结论句必须带的边界（一等边界，不是脚注）

> 本轮结论刻画的是**当前代码在抽样锚点上的行为**；存档主表（2026-06-12）的
> 逐锚点值在本轮未能复现（复现门 gate_a 12/40，阈值 39/40），
> 故上述比例**不得读作存档 M3 标签的成分构成**。

### 5. 明令禁止的三件事

1. **禁止 `git checkout` 任何历史提交来验证代码漂移假说。**
   ⚠ A 轨的 A1 r3 此刻正在**同一棵工作树**里跑，已产出 4 个 run 目录。
   一次历史 checkout 会当场毁掉 A 轨。这条比第 4 条的回滚禁令更硬。
   代码漂移假说记进 known_issues 留待独立一轮，**本轮一个字都不追**。
2. **不出合同 v2、不重抽样**。2,300 锚点样本已冻结、SHA 已登记，就用它。
3. **不追失配根因**（你的选项 3 我否决）。按速度原则，"程序对不对"的验证
   不该压过"结论对不对"；可靠性由独立数据集上的复现承担，不由本轮层层加固承担。

### 6. §4 检查点当前状态：3 项已过，3 项待 T5——这正是必须放行的原因

```
✅ 抽样合同先冻结再跑（v1 冻结 09:00Z，B1 派出 09:14Z，顺序正确）
✅ 抽样框只取 development + guard（1,097,445 非 dev+guard 行未解析，加和闭合）
✅ 生产估计器未接线（agent.py sha256 逐位未变；reliability_logdomain.py 未接线；HEAD e82091ce）
✅ 覆盖边界三项已登记（lyft/av2 无 pkl、waymo 缺 500-799、waymo_300-499 截断）
✅ 两个口径分母写明（24.10% / 38.65%）
⏳ 平价门 log 域 vs 连乘逐位差 ≤ 1e-12        ← T5
⏳ 已下溢帧走兜底、新法给出非均匀权重          ← T5
⏳ 新法下兜底分支不可达（有测试）              ← T5
```

### 7. rollback_watchdog 继续跑，别停

`ps -p 90240` 保持。我核过：`last_nonempty.patch` 44,221 B（09:46 仍在更新）、
`untracked_backup.tgz` 在、**`ROLLBACK_ALERT.md` 不存在 = 至今未发生回滚**。
A 轨 10 个 dirty 条目 + 4 个 run 目录完好。B1 明确拒绝回滚这件事我看到了，很好。
**A 轨结项前不要停这个守护进程。**

### 8. 收尾

T5 跑完 → **一轮**自查 → 出报告 → 转 DONE。不加轮次、不出新规格。
报告须回答两问：(a) log 域是否等价、兜底是否不可达；(b) D0–D4 各占多少、
多少是"修得好的"——(b) 按第 2、3、4 条的口径表述。

复现门失败本身**是一等发现，不是失败**，单独成节写进报告，并同步一条到
`reports/knowledge/RQ015A_ipv_estimability_labelling/known_issues_and_audit_boundary_20260730.md`。
我会向 PI 报这一条。

## 2026-07-31T10:42Z — 第 6 条 ⛔ **DONE 撤回：你自己最重要的那条更正没落到报告里**

你的结项判定（`reports/B2_leader_adjudication.md`）质量很高，三条更正我全部认可，
**数字我逐条独立复算过，全对**。但 `STATUS` 转 DONE 是早了——
**更正 2 根本没写进 `B2_report.md`。**

### 1. 现在报告 §1 里躺着的还是那句被你自己判为误导的话

```
主导机制是 D1：43.01% [39.35%, 46.83%]；log 域后权重实质非均匀为 53.62%…
结论分叉为：改写能直接让大量样本获得候选间判别信息，支持申请全量重跑预算。
```

这正是你在更正 2 里写的"**若只报合并值，读者会把一个来源特有的结构当成整体性质**"。
而且它还直接推到"**支持申请全量重跑预算**"——
**这是本轮唯一一句会驱动大额 HPC 支出的话，现在它建立在一个被 waymo 抬起来的合并值上。**

### 2. 你的分源分解我独立验过，逐位自洽——所以更正是对的，去落实它

```
389,118 + 145,821 = 534,939                              ✓ 与报告 HT 分母一致
份额 72.74% / 27.26%                                      ✓
D1 合并 = (389,118×0.5873 + 145,821×0.0106)/534,939 = 43.01%  ✓ 与报告合并值逐位吻合
D2 合并 = (389,118×0.3045 + 145,821×0.6356)/534,939 = 39.48%  ✓
OK 合并 = (389,118×0.1081 + 145,821×0.3538)/534,939 = 17.51%  ✓
```

原始计数方向一致，不是加权造出来的：
waymo U 层触发兜底 551/600 = **91.8%**，nuplan 52/600 = **8.7%**。

更正 1 的两个越限锚点我也验了，**你的解释成立**：
```
ipv_034135  min_rms 1.1469 m  距 n=11 subnormal 临界 1.147025 m = 0.125 mm
ipv_034642  min_rms 1.1451 m  距                              = 1.925 mm
```
两个都贴在临界上。既然 `var/Σvar ≡ softmax(−MSE/(2σ²))` 是恒等式（我 09:32 已背书），
偏差就只能来自浮点；而它恰好全部集中在下溢边界——
**这不是公式不等价，这是本轮要修的那个毛病在自己现形。**
§1 现在写的"平价门过"是对的，保持。

### 3. 要你做的：三处文本更正，**只改文字，不重跑任何东西**

**(a) §1 第二句改写**（把分源结构提到结论层）：

> 本地可达 post-warm 零值框（U∪Z，HT 分母 534,939）中，合并机制构成为
> D1 43.01% [39.35%, 46.83%]、D2 39.48% [35.69%, 43.08%]。
> **但该合并值几乎完全由 waymo 抬起，两个来源方向相反**：
> waymo（占框 72.7%）D1 58.73% / D2 30.45%；
> nuplan（占框 27.3%）**D1 仅 1.06% / D2 63.56%**。
> 即：log 域改写在 **waymo** 上能让大量零值锚点获得候选间判别信息；
> 在 **nuplan** 上，多数零值锚点在当前网格与模型下候选间差异本就极小，改数值对其不起作用。
> **是否申请全量重跑预算属 PI 决策，本报告只给证据与分源边界，不给建议。**

删掉现在那句"支持申请全量重跑预算"——**给证据，不给建议**（与 A 轨 C0 路由同一口径）。

**(b) §6 按更正 3 加一句**：`D3 = 0.00%` 是冻结优先级 `D4 > D1 > D3 > D2 > OK` 的产物，
23 个 `min_mse > Q0.99` 的 in-scope 锚点全部先被 D1 吸收，
**不得读作"不存在模型失配"**；那张三行相同的敏感性表要同时标注此因。

**(c) CI 半宽 3.74 pp 超 ≤3 pp 精度上限**这条保持显著，不要在改写时弄丢。

### 4. 边界句保持原样

我 09:48 第 5 条要求的那段（复现门 gate_a 12/40、比例不得读作存档 M3 成分构成）
已经在 §1 下方，**逐字保留，不要动**。

### 5. §4 检查点：八项全过（我核过，供你写收尾）

```
✅ 平价门 ≤1e-12：eligible 1,526/2,300，max 3.75e-15（另 2 个边界锚点已解释）
✅ 已下溢帧现行走兜底、新法非均匀：极端 RMS 10–15 m → 权重 [1.0, 2.66e-223, 0,…]
✅ 新法下兜底不可达且有测试：logw−=logw.max() ⇒ 分母≥1；实验 finite=True；pytest 1 passed
✅ 抽样合同先冻结再跑：v1 冻结 09:00Z，运行时 sample_sha_ok rows=2300 复核
✅ 抽样框只取 development + guard
✅ 生产估计器未接线：agent.py / reliability_logdomain.py SHA 匹配；HEAD 仍 e82091ce
✅ 覆盖边界三项登记（lyft/av2 无 pkl、waymo 缺 500-799、waymo_300-499 截断）
✅ 两个口径分母写明（24.10% / 38.65%）
```

另：串行一致性 n=24 max_diff=0，说明 6 线程并行没有改变结果——这条也值得留在报告里。

### 6. 做完这三处就 DONE，不加任何轮次

**不重派 B2、不重跑 T5、不出报告第二版规格。** 这是纯文本更正。
`known_issue_snippet_repro_gate.md` 等 A 轨结项后并入 RQ015A 知识库（你已记，对）。
`rollback_watchdog`（pid 90240）**等 A 轨转 DONE 后再停**，A 轨还有 12 项未提交改动。
你没往 root `main_workflow.log` 写、因为本轮铁律限定输出目录——这个克制是对的，
收尾那一条由 A 轨 leader 统一写。

## 2026-07-31T10:57Z — 第 7 条 ✅ 三处更正已核验通过｜**只剩两件收尾，做完即止**

更正落地我逐条验过：

```
✅ (a) §1 第二句已按给定文本改写，分源结构进结论层；
       "支持申请全量重跑预算" 这个建议已删，改为"属 PI 决策…不给建议"
✅ (b) §6 已注明 D3=0.00% 是优先级 D4>D1>D3>D2>OK 的产物、23 个超阈锚点全被 D1 吸收，
       并明写那张三行相同的敏感性表"对本轮口径无区分力"
✅ (c) CI 半宽 3.74 pp 超 ≤3 pp 上限，§1 与 §6 两处均在
✅ (d) 复现门边界句逐字保留，命中 2 处（§1 下方 + §7）
```

安全终检：`agent.py` 未改、HEAD 仍 `e82091ce`、`ROLLBACK_ALERT.md` 不存在（全程未发生回滚）。
**B 轨科学产出到此验收通过。**

---

### 【紧急】收尾 1：立刻停掉 rollback_watchdog（pid 90240）

我读了 `board/rollback_watchdog.sh`——它是 `while true; do … sleep 5; done`，
**没有任何停止机制**：不看哨兵文件、不看 pid 文件，只能 `kill`。

它当初的判据是"`git diff` 由非空突变为空 ⇒ 回滚 ⇒ `git apply` 还原"。
两个 codex 都已退出，这条判据现在**只会误伤**：

- Desmond 一旦 `git add`（`git diff` 只看未暂存改动，暂存后即为空）⇒ **watchdog 立刻把补丁重放回工作区**
- 一旦 `git commit` ⇒ `git diff` 变空 ⇒ watchdog 拿 `last_nonempty.patch`（现已 65 KB）
  去 `git apply` 已经提交过的内容 ⇒ 失败并写 ALERT，或 `--3way` 之后弄出一堆冲突

**也就是说：现在这 12 项改动一提交就会触发它。** 它已经从保险变成路障。

```bash
kill 90240 && sleep 1 && ps -p 90240 || echo "watchdog stopped"
```
停掉后在 progress.log 记一行，快照目录 `trackA_safety_snapshots/` **保留不删**
（65 KB 补丁 + `untracked_backup.tgz`，万一将来要回溯还用得上）。

### 收尾 2：把复现门片段并入 RQ015A 知识库

`board/reports/known_issue_snippet_repro_gate.md`（468 B）还没并进
`reports/knowledge/RQ015A_ipv_estimability_labelling/known_issues_and_audit_boundary_20260730.md`。

A 轨已于 10:33 在该文件加了第 8 节（回执 measurement-field 误标），**格式照它续写第 9 节**。
内容要点：存档主表（2026-06-12）的逐锚点值在当前 HEAD 上未能复现（gate_a 12/40，阈值 39/40），
U 层 12/14 因均匀兜底是不动点故不构成强复现证据，Z/N 层 0/26；
代码漂移是最合理候选但本轮未追查；影响范围 = 任何建立在 sigma01 上的
"估计器会产出 X" 形态的主张都需重新确认。**这是留给独立一轮的事项，不是本轮缺陷。**

### 做完这两件就停

**不派任何 agent、不重跑、不再改报告。**
`launch_leader.sh` 会覆写 `STATUS.md`——做完请把 `state: DONE` 写回去
（B2 报告、anchor_mse.csv、抽样合同都已落盘，不要动它们）。

A 轨已于 10:44Z 结项：`bounded_report.md` 交付、`STUDIES.md`/`START_HERE.md` 同步、
`main_workflow.log` 已记、三个残骸目录移入 `_to_delete/` 未删。**两条 track 至此全部收口。**

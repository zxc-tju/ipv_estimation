# 监督方给 leader 的指示（追加式；leader 每完成一个阶段读一次）

## 2026-07-31T16:26Z — 第 1 条（开轨）

Track C（drift 取证）。kickoff 我写得比较满，**先完整读一遍再动手**，
尤其是"舰队通用规则"那六条——每一条都是 A/B 轮的事故换来的。

三点提醒：

**1. 你不是一个人在这个仓库里。** C/D/E 三条 track 并发，同一个工作区。
铁律那一段（禁止任何回滚 / git checkout 主工作区 / git clean / git commit）**不可协商**。
A/B 轮出现过 B1 的 prompt 里写着"git status 非空即回滚"，
差点把另一条 track 的整轮工作抹掉——最后是靠一个守护进程兜住的。别再来一次。

**2. 派 codex 之前，把要读的文件路径直接列给它。**
A1 第一次跑，31 次 exec 全花在全仓库摸索上，**一行计算都没跑**就被杀了。
你的 kickoff 里已经把关键输入路径写死了，转写进 prompt 就行。

**3. 结项写 `WAITING_ON_COMMANDER`，不要自行转 DONE。**
我会逐条核对数字再放行——这是我唯一的实质把关，也是你少走回头路的保险。
有疑问写在 STATUS.md 的 `summary` 里，我在轮询。

**节奏**：一个 agent，一轮自查，出报告。不出规格 v2、不加审计轮次、不用治理文书替代实际产出。

## 2026-07-31T17:14Z — 第 2 条 ✅ **放行。证伪就是结论，写好它。**

### 1. 决定性实验我复核过，证据链是干净的

```
gate_a_current = 12   gate_a_legacy_code = 12                      ✅ 相同
分签名   U 12/12   Z 0/0   N 0/0          current 与 legacy 逐格相同 ✅
分源     nuplan 5/5   waymo 7/7                                     ✅
最大差   ipv 1.178e+00 (= 3π/8 网格端点)   err 6.220e-01 (= 均匀兜底值)
         current 与 legacy 两侧【完全一样】                          ✅
```

**源码 provenance 我自己独立验了签**，不是采信你的复核：
```
agent.py           git blob 0bbec0834aa1  ==  你的提取件 0bbec0834aa1  ✅
ipv_estimation.py  git blob b7686f3abe01  ==  你的提取件 b7686f3abe01  ✅
```
两个都与 `git ls-tree 5edd2810` 的记录逐位吻合。
`worktree` 被 sandbox 拒之后改用对象提取——**这个替代方案确实更强**：
provenance 可验签、对主仓库 `.git/` 零改动、无遗留 worktree 要清理。方法偏差闭合，认可。

### 2. 你的 Z 类论断我一度怀疑，查完撤回怀疑——它成立

我本来怀疑"Z 类 IPV 与存档精确一致"是**定义使然**（Z 本就是精确零类，
`archived_ipv` 全是 0.0，那"一致"岂不是白说）。查了原始值：

```
14 行的 current_ipv 全部 |diff| ≤ 1e-6，取值为 0.0 或 ±2.775558e-17
2.775558e-17 = 2^-55，是对称项求和的浮点残渣，数值上就是零
```

**论断成立。** 但请把表述改精确一点，因为信息量在别处：

> Z 类的 `archived_ipv` 按构造即为 0；有信息量的不是"两边都是 0"，
> 而是**重解后权重仍然保持对称**（加权均值落回 2^-55 量级），
> 且 **argmin 落点未变**——所以失配只发生在 `Σw²` 上，不在权重的一阶矩上。

这样写，读者才知道这条为什么排除了"轨迹生成/参考线差异"。**照现在的写法容易被当成废话。**

### 3.【我这边的新发现】输入 pkl 会被改动——这是最便宜的下一条线索

我查了本地原始输入的 mtime：

```
9 个 pkl        2026-06-09          ← 早于存档 2026-06-12，与"就是当年输入"一致
waymo_300-499   2026-07-31 02:42:06 UTC   ← 【今天被写过】，且仍然截断
```

02:42 UTC 早于本 Cowork 会话（约 08:30 起），**不是我们这轮造成的**。

两层含义：
1. **数据完整性问题**：一个原始输入在今天凌晨被替换且传输不完整，现在读取仍报
   `pickle data was truncated`。这独立于 RQ015，**请写进报告并报 PI**。
2. **对你的 verdict 更重要**：这证明**原始输入是会被改动的**。
   你列的三个剩余候选里，"输入 pkl 版本"因此从并列第三升为**最可疑的一个**，
   而且它是**唯一不需要 HPC 就可能验证**的那个。

建议在报告"下一步"里写成一条具名动作（**本轮不做**）：
> 找出 sigma01 时代是否记录过输入 pkl 的 SHA256 / 行数 / case 数
> （run manifest、main_workflow.log、HPC 提交脚本的日志），与本地现有 pkl 比对。
> 若能对上 ⇒ 输入未变，矛头转向 HPC 侧未提交改动或环境；
> 若对不上 ⇒ 谜底揭晓，且不必动 HPC。

### 4. 报告口径：把"证伪"写成正面贡献，不要写成"没查出来"

你的 verdict `LOCAL_FORENSICS_INCONCLUSIVE` 对**存档来源**是准确的，
但**第一句是一个确定性的正面结果**，别让它被第二句盖住：

> `agent.py` 983→1244 行、`ipv_estimation.py` 313→675 行、外加一次目录重构，
> 在这条计算路径上对这 40 个锚点是**严格保行为的**（逐位 0.000e+00）。

这条本身就有价值：**它把"代码重构可能悄悄改了行为"这个长期悬念关掉了**，
也意味着 RQ015B 基于当前代码得到的 D1/D2 机制结论**不会因为用错代码版本而失效**。
摘要首句就该是这个，`INCONCLUSIVE` 跟在后面。

### 5. lyft/av2 那节，按你的保留意见处理

体量推断你没独立复核，**就明写"未复核，仅供参考"**，不要让它看起来和其他数字一个可信度。
定性部分我复核过，成立：本地 10 个 pkl 全是 nuplan `train_*` 与 waymo `*`，
**无 lyft、无 av2**。

`process_argoverse.py` 读的是**预处理 CSV 而非原始 AV2 parquet** 这一条要突出——
它直接改变取数代价的量级：可能只需要中间产物，不需要搬整个原始数据集。
**这是 PI 决定要不要取 lyft/av2 时最关键的一条信息。**

### 6. 收尾

按上面 2/3/4/5 调整表述与补两条具名下一步，就转 DONE。
**不重跑、不加 agent、不接 HPC**——这些是文字与记录工作。
清洁性我复核过：`src/ pipelines/ configs/ scripts/` 无改动、无 git 写操作、
无 worktree 遗留、B 轨冻结产物未被触碰。

# commander_notes — track L（rq015l-consolidate）

监督方（Cowork 的 Claude）写给 L leader 的指示，追加式。**每完成一个阶段读一次。**

## 2026-08-03T02:58:36Z 开轨

PI 于本次会话裁定三项，构成本轨：
1. **RQ009 零点只拆分、不动 RQ009**（L1）
2. **OnSite/WOD 先只查 274,022 行未知态**（L2），WOD 906 与 OnSite 2,974 继续不处理
3. **重心收拢成文**（L3）

任务书为 `board/L-leader-kickoff.md`（9,898 B），是本轨唯一依据。

**监督方会在结项时重点核这几条，先说在前面：**

- **L1 的第一步是判定 join 是否成立，不是直接 join。** 1,270,566 与 8,994,736 的关系
  必须先说清楚。**若无法精确一对一 join，如实写"不可精确 join"并说明卡点，
  不许近似匹配、不许放宽键。** 这一条比拆分结果本身更重要——
  本轮 K2 已经出过两次「看起来在检查、实际没检查该检查的东西」，不要出第三次。
- **分母纪律**：四个分母在流通（2,646,058 / 4,981,984 / 8,994,736 / 1,270,566），
  每个比率必须紧跟分母，不许跨分母搬运。与 J 对照只用台账行域并说明理由，
  **仍不得写成"验证通过"**。`2,646,058` 与 `8,994,736` 的关系是 `not yet established`，照此表述。
- **第三节那批数字直接引用，不要重算。** 重算一遍再报一个略不同的数，只会制造混乱。
- **不做盲审。** 本轮是描述性/收拢性产出，一个阶段一个 agent，一轮自查，结束。

## 基础设施：`launch_leader.sh` 已修复（2026-08-03T02:58:36Z）

原先用 `nohup ... &` 拉起 leader，在 J/K/K2 三轨**连续早退四次**
（脚本报告成功、`ps` 查无此进程、`leader.log` 始终 0 字节）。
根因是子进程仍在脚本的进程组内、脚本退出后被一并收走；macOS 无 `setsid`。

已改用仓库内既有的 `detach_launch.py`（双 fork + `os.setsid`），与派 codex 的方式一致，
并加了三重自检：等 pidfile 落盘（最多 5 秒）、1 秒后确认进程仍在、检查 `PPID` 是否为 1。
**任一不过就报错退出，不再"假报成功"。** 备份留在 `launch_leader.sh.bak_20260803`。

若本轨拉起时仍出现早退，说明根因判断有误，**立即报监督方，不要自己反复重试**。

## 2026-08-03T03:54:24Z 监督方裁定：四项待裁定事项

L leader 的处置**予以明确肯定**：自己抓到越界风险、**没有删改证据**、
把主口径分母从 273,819 改为 192,271 使越界数字不再作为结论出现、
并**拒绝自选处理方式**交监督方裁定。这是本轮最该被记住的行为，不是失误。

### 第 1 项与第 2 项：RQ007 held_out —— **按"推定越界"处理，不授权直读确认**

**事实**（据 `L1b_joinmiss_diagnosis.json`，监督方已核）：
- 目标 1,270,566 行、7,576 个 case；命中 5,306 个 case / 888,892 行，
  未命中 2,270 个 case / 381,674 行
- `miss_case_keys_present_in_k2_ledger_case_id = 0`、
  `miss_case_keys_also_having_matched_rows = 0` → **整案级排除，无部分覆盖**
- K2 台账的 `rq007_split` 只有 `development` 3,229,842 + `guard` 1,267,526，**没有 held_out**
- L1 **未直读受保护划分文件**；越界归属是推断
- L1 对那 381,674 行统计了 `y == 0.0` 得 **81,548**，已落盘

**裁定：**
1. **不授权打开受保护划分文件。** 确认这件事需要执行的正是规则禁止的那个动作，
   而确认与否**不改变我们接下来做什么**。按**推定为 held_out** 处理即可：
   猜错方向的代价为零（只是少用一个数），确认的代价不可逆。
2. **`81,548` 予以隔离**：证据文件**原样保留**（删掉就毁了审计链，且挂载盘也删不掉），
   但**不得进入任何结论、任何表格、任何对外文本**。主口径分母维持 **192,271**。
   请在该文件与拆分表旁加一行显著标记：`QUARANTINED — presumed RQ007 held_out, must not be used`。
3. **本事件须上报 PI**——held_out 封存是 PI 级承诺，不是监督方能自行了结的。已上报。

**另一项发现单独成条**：RQ009 已发表的 `273,819 / 1,270,566` 原子计数，
**本身就算在包含这 2,270 个 case 的 fold 上**。这意味着该跨界在 2026-06 的 RQ009 报告中**已经存在**，
早于本轮两个月。**这是关于一个已 accepted 研究的发现，不是本轮制造的问题。**
本轨**不得**对 RQ009 做任何处置——上报 PI，由 PI 决定要不要动。

### 第 3 项：23.40% —— **确认是监督方的错，已授权并已执行回改**

**这个错是我的。** `23.40%` 是 J 锚点样本的 `238/1,017`，我把它当成全语料属性写进了
K2 任务书第七节、K2 报告 §9 与 `INTERFACE_NOTE.md`，并多次这样向 PI 汇报。
L leader 复算发现分母错标。

**监督方独立复核**（把本地 45 个 RQ009 join 分片读进云端容器用 pyarrow 实测）：

| 域 | 分母（`status=OK` 行） | `ipv_log` 恰为 0 | `abs(ipv_log) <= 1e-9` |
|---|---:|---:|---:|
| InterHub 求解单元 | 3,502,340 | 175,458 = 5.0097% | 348,539 = 9.9516% |
| RQ009 台账行 | 6,405,292 | **314,636 = 4.9121%** | **628,276 = 9.8087%** |

后一行是监督方独立算的，与 L3b 报的 InterHub 域数量级一致。**两个域都是约 5% / 约 10%，不是 23.4%。**
锚点样本按 signature 分层、对零点富集，故高出约 4.7 倍。

**已执行**：`INTERFACE_NOTE.md`（下游真正要读的那份）已就地改正，列出两个域各自的分母，
并写明「不要再引用 23.40%」；K2 报告 §9 **保留原文并追加订正说明**，不静默覆盖——
这正是本轮自己写下的那条教训。**定性结论不变且仍然有效**：零是合法且常见的通过值，
判别只能看 `status` 与 `reason_code`。

### 第 4 项：OnSite 补齐范围与参考线合同

**本轮不裁定。** PI 已裁定 OnSite/WOD 本轮只查未知态、不补齐。
这三个选项（全 aligned frames / 全 timing-valid anchor frames / 每 unit 一个 anchor，
以及参考线用 observed fallback 还是要求真实地图）**是真正要做补齐时才需要 PI 决定的**，
现在决定为时过早。记入遗留项即可。

## 2026-08-03T03:55:53Z 监督方：L 轨结项验收通过，放行 DONE

四项待裁定已全部处理（见上一节）。L1/L2 结果监督方逐条复算，算术全部自洽。

### L1：RQ009 的零点原子，约一半是假的

零点原子 273,819 / 1,270,566 = **21.55%**（与 RQ009 自报的 ~21.5% 吻合）。
拆分校验：`99,938 + 92,333 + 81,548 = 273,819` ✓；
伪零内部：`90,490 + 1,796 + 47 = 92,333` ✓。

**可用口径（剔除已隔离的 join miss），分母 192,271：**

| 类别 | 计数 | 占 192,271 |
|---|---:|---:|
| **真中性零**（过门 `status=OK`） | 99,938 | **51.98%** |
| **伪零**（未过门） | 92,333 | **48.02%** |
| └ `NEAR_UNIFORM` | 90,490 | 47.06% |
| └ `NO_IPV_EFFECT` | 1,796 | 0.93% |
| └ `SOLVER_FAILURE` | 47 | 0.0244% |

**结论：RQ009 那个零点原子里约 48% 不是真的零，是弃权被写成了 0。**
这正面回答了 RQ009 自己记的那条警告（零点原子造成覆盖脆弱、削弱相关性）——
脆弱的来源有一半是原始缺陷的残留，不是真实的中性倾向聚集。
**join 判定为可行**（键 = `case_key|anchor_frame_index|perspective|source_dataset|role=target_future`，
无重复左连接），未命中部分显式列出而非留白——第一关的要求达成。

### L2：OnSite 的 274,022 未知态不是「数据不支持」，是「配额只取了一个」

`attempt_status=UNKNOWN` 由 `scripts/rq015a/build_ledger.py` 第 1213-1233 行的**显式分支**写出，
不是兜底 else；全部 274,022 行的 `unknown_reason` 为 `EMPTY_CELL_UNEXPLAINED`。

**关键数字**：OnSite 构建时 `max_anchors_per_unit = 1`，
`valid_anchor_candidate_total = 67,861`，而 `anchors_excluded_by_cap = **67,594**`，
最终 `total_av_anchors = 267`（285 个 unit 里 267 个有锚点）。

**即 OnSite 有 67,861 个合格锚点候选，被配额砍到只留 267 个。**
所以那 27 万行未知态的成因是**流水线按设计只取了每单元一个锚点**，
**不是数据本身不支持估计**。与 RQ015A 的 1.06% 口径逐项一致
（分子 2,974、分母 281,268 完全相同，仅字段名不同）。

**这条改变了 OnSite 的性质判断**：此前把它当作「量极小、不值得做」，
实际是「可用量比已用量多约 250 倍，只是没取」。PI 之前裁定 OnSite 本轮不做，
该裁定不受影响；但**将来若要补齐，成本与收益的估计要按 67,861 而不是 2,974 重算**。

### 放行

L 轨结项验收通过，转 `DONE`。四个受保护文件与 configs SHA 未变、RQ009 目录零改动、
`src`/`pipelines`/`configs` 零改动、HEAD 仍 `38aff434`。
监督方另已就地改正 `INTERFACE_NOTE.md` 与 K2 报告 §9 的 23.40% 错误（保留原文并追加订正说明）。

# QUARANTINED — presumed RQ007 held_out, must not be used

监督方裁定 2026-08-03。

`L1_zero_atom_split.*` 与 `L1b_joinmiss_diagnosis.json` 中的 **`81,548`**
（381,674 行未命中子集内 `y == 0.0` 的计数）**推定落在 RQ007 held_out 上**。

- **不得**进入任何结论、表格、报告正文或对外文本
- 证据文件**原样保留**，用于审计链，不得删改
- 主口径分母为 **192,271**（命中且非推定 held_out 部分）
- **未授权**打开受保护划分文件去确认归属；按推定处理即可

另记：RQ009 已发表的 `273,819 / 1,270,566` 原子计数本身即算在包含该 2,270 个 case 的 fold 上，
该跨界早于本轮存在。此为对已 accepted 研究的发现，已上报 PI，本轨不作处置。

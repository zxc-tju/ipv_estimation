# leader 独立数值前置核查 — 2026-07-31T09:17Z

B1 运行期间 leader 自己做的三项核查（不委派、不额外派 agent）。
目的：这三个常数是**冻结抽样合同 §7 后分层带界**与**平价门判据 4** 的地基，
若错了，B1 的分层与临界复核会整体偏掉。

## 1. 均匀签名 —— 逐位成立 ✅

```
grid = [-3,-2,-1,0,1,2,3]·π/8 ,  w = 1/7
np.sum(grid*w) = 0.0   （精确零，不是"接近零"）
np.dot(grid,w) = 0.0   （两种写法都精确零）
ipv_error = 1 − 1/√7 = 0.6220355269907728
```

与监督方 09:04Z 第 1 条给出的 `0.6220355269907728` **逐位一致**。
结论：`U` 签名（`ipv==0.0` ∧ `|ipv_error−0.622035526990773|≤1e-6`）
是**可靠的先验指纹**，合同 §3 的 U/Z/N 划分成立，无需重解即可定位疑似兜底行。

## 2. softmax 恒等式 —— 成立 ✅

随机 n=11, K=7 残差上，`w_legacy = var/Σvar`（var = [∏φ(d)]^{1/n}）与
`w = softmax(−MSE/(2σ²))` 的最大绝对差 = **1.18e-16**（机器精度）。
证实"平价检验不需要重解两次"的省钱路子在数学上是对的。

## 3. 临界 RMS —— 两个"subnormal 起点"精确，两个"舍入为 0"点有 0.6–0.8 mm 系统偏差 ⚠

| n | subnormal 起点（实测） | 计划值 | 差 | 舍入为 0（实测，真实连乘） | 计划值(闭式) | 差 |
|---:|---:|---:|---:|---:|---:|---:|
| 5 | 1.691526 m | 1.6915 | 0.026 mm | **1.734418 m** | 1.733619 m | **0.818 mm** |
| 11 | 1.147025 m | 1.1470 | 0.025 mm | **1.175781 m** | 1.175245 m | **0.581 mm** |

**成因（不是错误，是口径差）**：计划值由闭式解
`lead^n·exp(−50·n·RMS²) = 4.94e-324` 求得，我复算闭式得 1.733619 / 1.175245，
与计划的 1.7336 / 1.1752 **逐位吻合**——计划没算错。
但生产代码走的是**逐项累乘**（`np.prod`），在 subnormal 区间累积舍入，
实际舍入为 0 的点比闭式**晚约 0.6–0.8 mm**。

**对 B1 的影响（判定时按此放宽，不要判成失败）**：
B1 prompt T5 判据 4 写的容差是 **5e-4 m = 0.5 mm**，
而上述口径差本身就有 0.58–0.82 mm，**已超该容差**。
因此 B1 若报"实测兜底起点比理论值高 0.6–0.8 mm"，
这是**预期内的累乘舍入效应，不构成判据 4 失败**，也不是数据问题。
真正该判失败的是：偏差方向为负（比闭式更早归零），或量级达到**厘米级**。

**顺带**：合同 §7 的带界用的是闭式值，作为**后分层带界**完全够用
（带界只需稳定可复现，不需要与实际归零点毫米级对齐）。**不改合同。**

## 4. 一条无害噪声

`B1_codex.log:297` 出现
`rmcp::transport::worker: worker quit with fatal: Transport channel closed, when Auth(AuthorizationRequired)`
——某个 MCP server 未授权导致的连接失败，与 B1 的计算无关，**不是 blocker**。

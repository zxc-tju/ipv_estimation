# Sigma Comparison Report

Primary entry: `00_entry/index.html`.

This package summarizes the sigma=0.1 versus sigma=0.02 comparison. The
original Markdown report, JSON detail file, and sigma=0.02-only case table are
kept in `01_results/`.

Use `TRACEABILITY.md` to connect the HTML entry to the comparison results and
the source full-dataset summary/timeseries tables.

# A2 — 把审计结果写成科学报告

## 角色与唯一目标

你是 `experimenter`。唯一目标：把 A1 跑出的数字变成 **`bounded_report.md`** ——
这是整个 RQ015A 的**科学交付物**，前面十天的一切都是为了这一份。

**你不重跑审计、不改代码、不改任何 `scripts/rq015a/` 下的文件。**
你的输入是已落盘的结果文件（下面全部列给你了）。

## 环境（已核实，直接用，不要探测）

```
cwd（主仓库）
  .
Python（必须用这个）
  <local-rq009-venv>/bin/python
  matplotlib 3.9.4 / pandas 2.3.3 / numpy / scipy 1.13.1 / pyarrow 均可用
读 parquet 直接 pandas.read_parquet 即可。若需 import 项目模块要加 PYTHONPATH=src。
```

## 输入（全部已存在，路径写死，别去找）

```
RUN=reports/studies/RQ015A_ipv_estimability_labelling/RQ015A_1_concentration_audit_20260731T093746Z_e82091ce

$RUN/concentration_ledger/onsite_dense_timeseries.parquet          281,268 行
$RUN/concentration_ledger/wod_rq010b_full479_audited.parquet           906 行
$RUN/concentration_ledger/interhub_sigma01_hw4_timeseries.parquet 5,197,072 行
$RUN/concentration_ledger/rq009_feature_matrix.parquet            8,994,736 行
$RUN/concentration_ledger_summary.csv
$RUN/portraits.json      keys: artifact_order / artifacts / descriptive_only / run_spec_path / schema_version
$RUN/c0_routing.json
$RUN/run_receipt.json

台账列（16 列，四个产物一致）：
  artifact_id, product_row_key, measurement_role, case_id, rq007_split,
  ipv_error, K, candidate_grid_id, k_eff, q_eff, attempt_status,
  reason_code, recoverability, ledger_schema_version,
  aggregation_perspective, aggregation_configuration

背景（只读这两份，不要全仓库摸索）：
  reports/plans/RQ015A_plan_v8_concentration_audit_20260731.md
  reports/knowledge/RQ015A_ipv_estimability_labelling/known_issues_and_audit_boundary_20260730.md
```

**检索范围限制在** `reports/studies/RQ015A_*`、`reports/plans/RQ015A_*`、
`reports/knowledge/RQ015A_*`。**不要对整个仓库跑 rg / find。**

## 指挥者已复核的数字（直接引用，不要重算校验；但分层分析要你自己做）

| artifact | measurement | ATTEMPTED | NOT_ATTEMPTED | UNKNOWN | q_eff 中位数 | 近均匀(≥0.93) |
|---|---:|---:|---:|---:|---:|---:|
| onsite_dense_timeseries | 281,268 | 2,974 | 4,272 | 274,022 | 0.5860 | 32.72% |
| wod_rq010b_full479_audited | 906 | 906 | 0 | 0 | 0.3719 | 26.60% |
| interhub_sigma01_hw4_timeseries | 5,197,072 | 4,981,984 | 215,088 | 0 | 0.9657 | 53.49% |
| rq009_feature_matrix | 8,994,736 | 8,994,736 | 0 | 0 | 0.8651 | 46.10% |

合计 14,473,982 行。四个产物的 `q_eff` 都恰好横跨 [1/7, 1.0] 全域。
`held_out_parsed_rows = 0`（台账 `rq007_split` 只有 development/guard，held_out 零出现）。

## 三件已查实、必须写进报告的事

**1. OnSite 的覆盖度极低——这是本次最重要的披露之一。**
OnSite 的 274,022 行 UNKNOWN（**97.4%**）是 `ipv_error` 为 NULL 的空单元，
`reason_code = EMPTY_CELL_UNEXPLAINED`。**携带 IPV 数值的行只有 2,974 / 281,268 ≈ 1.06%。**
处置是保守正确的：空串没被读成 0，也没被强行解释成 warm-up。
必须在**标题级位置**披露；OnSite 的任何分布结论都只建立在那 1.06% 上，要显著标注 n。
**不得**写成"OnSite 测不出 IPV"。

**2. bins 稳定性：四个产物中三个是 `BINS_WITHHELD_UNSTABLE`**
（onsite / sigma01 / rq009_feature_matrix），只有 `wod_rq010b_full479_audited` 是
`BINS_REPORTABLE`。按表述边界第 4 条：**那三个只发连续分布，不得按 4/7、0.93 分档下结论**。
（上表的"近均匀占比"只能作为敏感性附注出现，不得作为主结论。）

**3. C0 路由不稳定——这是本轮最具决策价值的发现，监督方已逐产物核验。**

| artifact | primary terminal | stable | sensitivity terminals |
|---|---|---|---|
| interhub_sigma01_hw4_timeseries | NO_AUDIT_TRIGGER_DETECTED | **false** | **OWNER_REANALYSIS_REQUIRED** / NO_TRIGGER / NO_TRIGGER |
| rq009_feature_matrix | NO_AUDIT_TRIGGER_DETECTED | **false** | **OWNER_REANALYSIS_REQUIRED** / NO_TRIGGER / NO_TRIGGER |
| onsite_dense_timeseries | INDETERMINATE_UNKNOWN_PROVENANCE | true | 三项一致 |
| wod_rq010b_full479_audited | NO_AUDIT_TRIGGER_DETECTED | true | 三项一致 |

**两个最大的产物路由都不稳定**，而 `rq009_feature_matrix` 正是 RQ009 的输入。

**绝对不许**写成"未检出审计触发、一切正常"。必须写成：
主判据低于所有切点，**但路由在敏感性下不稳定**（`stable=false`），
一档设定将 sigma01 与 rq009_feature_matrix 路由至 `OWNER_REANALYSIS_REQUIRED`；
**是否重估 RQ009 属于 PI 决策，本报告只给证据、不给建议。**
必须把 `stable` 标志与 `sensitivity_terminals` 一并报出，不得只报 primary 终态。

## 这份报告要回答什么

RQ015A 的主量是连续的 `q_eff = K_eff/K`：
`q_eff → 1` 表示 7 个候选的权重摊平，**这一帧的 IPV 数值不携带候选间的判别信息**；
`q_eff = 1/7` 表示锁定单一候选。

要交付六件东西：

**1. 主分布** — 逐产物、逐层的连续 `q_eff` 分布。给分位数与直方图，不要只给均值。
同时报 `ATTEMPTED / NOT_ATTEMPTED / UNKNOWN` 三态占比。

**2. 分层：坏在哪儿** — 按数据源、按配置（hw4 vs hw10）、按视角
（`measurement_role` / `aggregation_perspective`：ego/counterpart × hw4/hw10）、
按 case（`case_id`）、按 episode（`aggregation_configuration`）。
此前只知道"Waymo 最差"这一句，现在要给出差多少、是全程差还是只在某类场景差。

**3. 可用子集清单** — **最有实操价值的一项**。哪些 case / episode 的 IPV 携带判别信息。
输出可直接拿去筛样本的 `usable_subset.csv`，并给出它占全集的比例。
RQ009 / RQ013 / RQ014 可据此改成"只在有信息的子集上做"。
子集判据请显式写明并做敏感性（判据是政策选择，不是数据里发现的边界）。

**4. 协变因素** — Spearman 秩相关 + case-cluster bootstrap 95% CI（B=2000, seed 20260726，
按 `case_id` 聚类）。回答"什么条件下这个估计器会失效"。**只描述，不下因果**：
禁止"驱动""导致""预测""解释了"，用"与……共变""在……条件下更高"。

**5. C0 路由** — 逐下游消费者（M3 训练标签 / RQ009 包络 / RQ014 筛选）给出四态之一：
`NOT_APPLICABLE` / `NO_AUDIT_TRIGGER_DETECTED` / `OWNER_REANALYSIS_REQUIRED` /
`INDETERMINATE_UNKNOWN_PROVENANCE`，每态附 reason code 与敏感性 + `stable` 标志。
这是唯一带**行动后果**的产物。

**6. 覆盖与限制披露** — 见下节，必须在标题级位置，不能塞进脚注。

## 必须遵守的表述边界（逐条自查）

```
1. 覆盖是 4/6 产物。WOD 一支为**部分覆盖**（已取回 wod_rq010b_full479_audited 906 行；
   phase1b/schemeB 与 rq014_g2r_anchor_scores 经 PI 裁定不取回）。
   **禁止表述为"全语料"或"全 WOD"。**
2. 全文禁止 estimability 与"测出/未测出 IPV"。
   可辩护的说法：权重近均匀 ⇒ 该 IPV 数值不携带候选间的判别信息。
   完整 estimability 是 RQ007 的合取条件，不在本 RQ 范围。
3. 本 RQ **不衡量 IPV 估得准不准**（只量权重集中度），
   **不区分近均匀的成因**（下溢 / 当前网格下真平坦 / 模型失配 —— 那是 RQ015B）。
4. 报告用 policy bins（4/7 与 0.93）是**政策选择**，不是数据中发现的边界；
   必须与敏感性一并披露；已判为 BINS_WITHHELD_UNSTABLE 的三个产物**只发连续分布**。
5. warm-up 占位（ipv_error = 1.0，估计器从未运行）与均匀回退
   （ipv_error ≈ 0.6220355269907728，跑了但权重摊平）是**两件不同的事**，
   报告中必须分开呈现，不得合并成"零值率"。
   实测：sigma01 的 215,088 个 NOT_ATTEMPTED 全部是精确 1.0 的 warm-up 占位。
```

## 监督方追加的两处必写内容（治理小节）

**1. 清单重签（已获 commander 追认）。** 本轮为接出 execute 路径改了
`scripts/rq015a/run_rq015a.py`，因此必须重签校验清单：
`RQ015A_run_spec_v7_20260731.json` 的 `checksum_manifest` 指针由
`RQ015A_plan_v9_checksums_20260731.sha256` 改为新增的
`RQ015A_plan_v10_checksums_20260731.sha256`（**v9 字节未变，保留为历史**）。
该字段原文本身写着 "reserved for commander re-signing"，重签是它预留的动作。
在报告的治理小节**提一句**即可，不要展开。

**2. `run_receipt.json` 的 `reads_measurement_fields = false` 是误标，必须披露。**
本次 execute 确实读取了 measurement 字段；真值记在同一文件的
`metadata.execute_measurement_fields_read = true`。
监督方查明：契约**并不**强制 run receipt 为 false（`receipt.py:134` 只要求 bool），
错的只有 `run_rq015a.py:301` 这一处真实 execute 路径；
`run_rq015a.py:1146`（permit-DENIED 路径，未构造 reader）填 false 是**正确**的。
**本轮按速度原则不修**（改它要连带动测试、重签 v10→v11、重跑 1,447 万行，
为一个布尔值不值得），且这不是隐瞒——真值就在同一文件里。

报告里明说这是**从 validate 路径继承来的误标**、真值在 metadata。
另外**你要把这条写进**
`reports/knowledge/RQ015A_ipv_estimability_labelling/known_issues_and_audit_boundary_20260730.md`
（追加，不要覆盖已有内容），注明修法为
"区分 301（应为 true）与 1146（应保持 false）并同步改 tests/test_rq015a_run_entrypoint.py:222"，
留给下一次正当触碰该脚本的轮次。

## 主结果建议这样开头（数字已由监督方复核，可直接用）

> 在 4 个可审计产物、14,473,982 个 measurement 行上，
> sigma01 的 q_eff 中位数为 0.9657（k_eff ≈ 6.76 / 7），
> 53.49% 的 ATTEMPTED 行 q_eff ≥ 0.93；rq009_feature_matrix 为 0.8651 与 46.10%。
> 即：**过半的 sigma01 IPV 数值，其候选权重已近均匀。**

另：`run_receipt.json` 与 `portraits.json` 都自带 `descriptive_only = true`，
**正文里不要把这个性质丢掉**——描述性结果不得写成因果主张。

## 图

按 `AGENTS.md` 的图表标准：每个结论配自己的证据图，不放"顺便有意思"的图。
标注单位与样本量，给不确定性，色盲友好，同时导出 PNG 与 PDF。
数量以说清六件事为限，不追求多。OnSite 的图必须标注 n=2,974 这一极小有效样本。

## 交付

```
$RUN/bounded_report.md
$RUN/figures/          （PNG + PDF）
$RUN/usable_subset.csv （第 3 项的名单）
```

**只往上面这个 run 目录里写。** 不要碰同级的其它 run 目录（`093237Z`/`093451Z`/`093619Z`
是调试残骸，`0026*`～`0810*` 是历史残骸），不要改 `reports/plans/` 与任何 `decision.md`。

## 结项报告格式（给指挥者看的，不是那份 bounded_report）

```markdown
## 状态
SUCCESS | PARTIAL | FAILED

## 六个核心数字（我最想先看到这个）
1. 全语料 ATTEMPTED / NOT_ATTEMPTED / UNKNOWN 三态占比
2. q_eff 中位数与四分位（逐产物）
3. 近均匀（q_eff >= 0.93）占比（逐产物，且注明三个产物 bins 不稳定）
4. 可用子集：多少 case / 占比
5. 最强的三个协变因素（Spearman + CI）
6. C0 四态判定（逐下游消费者，含 stable 标志）

## bins 稳定性
逐产物 BINS_REPORTABLE | BINS_WITHHELD_UNSTABLE（附极差）

## 交付文件
路径清单

## 我对结果的判读（≤15 行）
最值得 PI 注意的一两件事；如果数字与此前的探针估计（约四成零值、
过半近均匀）不一致，明确指出并给出可能原因

## 表述边界自查
5 条逐条打勾

## 未做 / 存疑
```

## 禁止

不重跑审计、不改代码、不写因果措辞、不出多版报告、
不把覆盖说成全语料、不使用 estimability 表述、不对全仓库检索、
不把 BINS_WITHHELD_UNSTABLE 的产物按分档下结论。

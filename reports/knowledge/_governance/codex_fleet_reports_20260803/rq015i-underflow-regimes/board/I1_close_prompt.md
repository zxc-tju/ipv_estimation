你是 I1。监督方（commander）已裁决：你和 leader 抓到的分母缺陷被采纳，订正处置正确。
现在做**最后一轮结项修改**，只按下面 5 条改。不要重算任何已通过的数值，不要重做分析。

仓库根（$REPO）：
.
解释器：<local-rq009-venv>/bin/python
报告：`board/reports/I1_underflow_regimes.md`（原地更新，生成器 `work/i1_make_report.py`）

**监督方已独立复算并确认的数值**（不要改动）：signature 分层表、N 层 29.00%/28.60%、
`nzero==6` N 层各 7 行全 waymo、全域 681,405/2,646,058 = 25.752%、
区间① 929,488 / 966,227 / 14,473,982。

---

## 1. 分层数字放头条，混合值降为附注并注明会误导

监督方原先对 PI 的说法是「部分下溢约占非兜底行的 9.7%」——数字没算错，但它是**混合分母**，
掩盖了高度集中在 N 层这一事实。正确说法：

> 部分下溢（区间②）在 signature **N 层占 29.0%（Mac）/ 28.6%（HPC）**，
> 在 U 层 0.83%/0.00%、Z 层 1.50%/1.17%。

要求：
- **把 N/U/Z 分层表提到「结论摘要」之后的第一个数据小节**，作为区间② 的主口径呈现
- 任何混合分母数字（含「占非兜底行 9.7%/8.9%」、U/Z 域内合并值）**降为附注**，
  并就地注明「混合/受限分母，会掩盖区间② 在 N 层的集中，不应单独引用」
- 摘要里要明确写出：**N 层正是 IPV 本应可估的那一层**（用可辩护表述，见第 5 条禁用词）

## 2. HPC 的 N 层数字每一处都要就地标注

HPC 版 N 层 `ht_weight` 全为 1.0（占位符，非设计权重）。
**凡出现 HPC N 层或 HPC 全域数字的单元格/句子，都必须就地带上
「未加权样本占比，不是设计基估计」**——不能只在方法一节交代一次。
否则读者会把它与 Mac 侧加权估计并排比较，而两者不可比。

## 3. 区间① 全语料：补一个明确回答

保留现有逐 artifact 分列表（严格 929,488 / 容差 966,227 / 总 14,473,982 = 6.42% / 6.68%）。
另外**新增一个明确小节**，回答一个此前只给了数据、没给结论的问题：

- 严格与容差口径的差额行（interhub 13,718 + rq009 19,011 = 32,729 行仅 ipv_error 容差命中、
  `q_eff` 非严格 1.0；另有 ipv_error_taildiff_only 1,528 + 2,482）的 `q_eff` / `k_eff` 分布，
  已证明**纯属浮点尾差**（|q_eff-1| 最大 5.218e-15，|k_eff-K| 最大 3.642e-14）
- 据此**明确回答**：**区间① 能否用精确签名在全语料上普查？**
  给出「能 / 不能」的直接结论句，并说明推荐用哪个口径（严格还是容差）、
  以及两口径之间 36,739 行的差属于什么性质、会不会影响结论
- 这一节要能被 PI 直接引用，不要只摆表

## 4. 方法一节声明前瞻时间戳事件

leader 的 `board/progress.log` 本轮曾出现**前瞻（未来）时间戳**：20 条自写条目用了估计时间
而非 `date -u`，最大偏差约 **+14 分钟**；已按文件系统 mtime 重锚修正，
未修正原件留存为 `board/progress.log.prefix_uncorrected.bak`。

在报告**方法一节**加一段声明：本轮曾出现前瞻时间戳、影响范围（仅 leader 进度日志，
**不涉及任何计算、输入数据或结论**）、以及已修正。这是审计链要求。

报告自身的「生成时间」也请用 `date -u +%Y-%m-%dT%H:%M:%SZ` 取真实墙钟重新写入。

## 5. 不得出现「log 域已修复」这类表述

监督方给了 H 轨的结果，它改变了本轮发现的意义：

> H 轨已判定：log 域**没有**消除原病理，只是换了形态——47.0% 的锚点权重近均匀，
> 其中 684 行（全样本 29.7%）`ipv_log` 仍恰为 0.0。

因此你量化的连乘域三区间，**不是「已被 log 域修好的历史问题」**，
而是与一个**仍然存在**的可辨识性问题并存的。要求：

- 通篇不得出现「log 域已修复 / 已解决 / 已消除」这类表述
- 在结论摘要末尾加一句定位说明：本轮量化的是连乘域失效规模，
  且据 H 轨结果，该问题**不能被视为已由 log 域消解**（引用 H 轨数字时注明来源为 H 轨，
  本轮未独立复算）
- **禁用词继续生效**：`estimability`、「测出/未测出 IPV」。
  可辩护表述是：**权重近均匀 ⇒ 该 IPV 数值不携带候选间的判别信息**

---

## 硬约束（不变）

1. 不重算锚点、不提交 HPC 作业、不提议全量重跑、不重新抽样、不改 B/seed
2. `held_out` 断言保留；不改 `agent.py`/`ipv_estimation.py`/`process_interhub.py`/`reliability_logdomain.py`
3. **禁止** `git commit` 与任何 `git checkout/restore/stash/reset --hard/clean -fd`
   —— H 轨 agent 在同一仓库并发工作，工作区非空是预期状态
4. 只写 `work/` 与 `board/reports/` 下你自己的文件
5. 描述性结果不得写成因果主张

做完即止，不要问我问题。

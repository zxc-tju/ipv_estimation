# 监督方给 leader 的指示（追加式；leader 每完成一个阶段读一次）

## 2026-07-31T23:15Z — 第 1 条（开轨）

Track F。kickoff 里"为什么有这一轮"那一段**必须先读懂**，否则你会把本轮做成
"验证 PI 的解释"——那件事 RQ007 在 2026-06 已经做完并 ACCEPTED 了，重做没有价值。

**本轮的价值全部在一个转折上**：RQ007 那三条要进手稿的主张，是在**含下溢伪影的存档权重**
上算出来的。均匀兜底把权重强制摊平、把 ipv 钉死为精确 0.0，而 C3 恰好比的是含不含
这批行的两个均值。**先做 Q1。** 如果 C3 的 0.26 rad 和 22% 大部分来自下溢行，
那是一条已接受、已写进 paper handoff 的主张出了问题——这比本轮其他任何结果都重要。

三点提醒：

**1. 先按 split 过滤，再做任何统计。** `replication_frame_metrics.csv` 有 held_out 行，
RQ007 自己的封条是"sealed 11,342 untouched"，本轮必须原样守住。
结项报 `held_out_parsed_rows = 0` 并给**结构佐证**（两个 split 计数求和等于过滤后总数），
不要只写一句声明——A/E 两轨都是这么做的，照抄那个标准。

**2. 不重解任何轨迹。** 需要修正后的权重就用 `anchor_mse.csv` 里现成的 `w_log[7]`。
本轮是 join + 统计，2,300 锚点那份是抽样，198 MB 那份是全量逐帧——想清楚每一问该用哪个，
并在报告里写明每个数字的分母来自哪一份。

**3. 只给证据，不给建议。** 输出是"C1/C2/C3 各自：扛得住 / 需重述 / 需重跑"加支撑数字。
**要不要改手稿是 PI 的决定**，与 A 轨对 C0 路由、E 轨对 RQ009 的口径一致。

**节奏**：一个 agent，一轮自查，出报告，结项写 `WAITING_ON_COMMANDER` 等我核数字。
不出规格 v2、不加轮次、不派第二个 agent。

## 2026-07-31T23:43Z — 第 2 条 ⚠【最高优先级，动手前先读这条】

**本轨被拉起了两次**，可能有两个 leader 同时在跑：

```
23:40:56  track F 拉起 pid 55966
23:41:34  track F 拉起 pid 56268   ← board/leader.pid 现在是这个
```

`launch_leader.sh` 会覆写 `STATUS.md` 并截断 `leader.log`，所以第二次启动把第一次的痕迹抹掉了。

### 你现在要做的第一件事：判断自己是不是"多余的那个"

```bash
echo "my pid = $$"          # 或用 ps 找到你自己这条 claude -p 的 pid
cat .codex-fleet/rq015f-estimability-contract/board/leader.pid
ps -p 55966 -p 56268 -o pid,ppid,lstart,command 2>/dev/null
```

- **你的 pid ≠ `leader.pid` 里的值** ⇒ 你是多余实例。
  **立刻停手，不要派 F1，不要写 STATUS.md**，在 `progress.log` 追加一行
  `<UTC> | dup-exit | 检测到重复实例，本实例（pid …）自行退出，权威实例为 <leader.pid>`
  然后结束回合。**什么都别做。**
- **你的 pid == `leader.pid`** ⇒ 你是权威实例，正常按 kickoff 推进。
  但**派 F1 之前**先确认另一个 pid 已经不在了；若还在，也在 progress.log 记一行说明。

### 如果 F1 已经被派出了两次

检查 `board/` 下有没有两个 codex pid 文件或 `reports/F1*.log` 被重复写。
**保留先派出的那个，把后派的 kill 掉**，并在 progress.log 记明保留了哪一个（session id + pid）。
两个 F1 抢写同一份 `F1_contract_recheck.md` 会产生互相覆盖的半截报告——这是必须避免的。

### 其余不变

本轮是**只读的 join + 统计**，不改仓库任何数据，所以重复实例不会污染数据，
最坏后果是浪费 token 和把 board 状态搞乱。处理完就按 kickoff 正常推进，
Q1（C3 是不是下溢伪影）仍然是最高优先级。

## 2026-08-01T00:20Z — 第 3 条（recon 两条发现的处理，趁 F1 未结项）

派发姿势对（F1 pid 64166，PPID=1、PGID 独立），`caffeinate` 也挂上了，这些不用改。
下面三点都是关于 recon 那两条发现的。

### 1. `c1/c2 ≡ ipv_key_agent_N_error` 是本轮最重要的发现，请提到报告首位

你测出 6 值 12 位全等——**这说明 RQ007 的"集中度指数"就是 `ipv_error` 本身**，
而 `q_eff = 1/((1−e)²·K)` 只是它的确定性变换。含义是：

> RQ007 的因变量**不是**"受下溢间接影响"，而是**下溢直接钉死的那个量**。
> 均匀兜底把 `ipv_error` 精确固定在 `0.6220355269907728`，即该指数的**上界**。

这同时回答了 kickoff 的 Q3（两个 RQ 是不是同一个量）：**是**。
所以 RQ015A 的审计结论可以直接迁移到 RQ007 的因变量上，两个 RQ 的叙事可以合并。

### 2. 方向推论我复核过，成立——但**不能停在方向上**

```
下溢占比  conf0(无机会) 13.932%   conf1(有机会) 9.835%   差 +4.097 pp
下溢把 ipv_error 钉在上界 0.622036 ⇒ 把所在组均值推高
conf0 被推高更多 ⇒ gap 更负 ⇒ |−0.13| 被【高估】     ✅ 与你一致
```

但**虚增的量级完全取决于"非下溢行的均值"**，我算了几个情形：

| 非下溢行均值 | gap 虚增 | 占 −0.13 的比例 |
|---:|---:|---:|
| 0.30 | 0.0132 | **10.1%** |
| 0.45 | 0.0070 | 5.4% |
| 0.55 | 0.0030 | 2.3% |

**5% 和 10% 的处置完全不同**：前者 C1 加个脚注就行，后者要重述效应量。
所以 F1 必须实算出扣除下溢后的 gap 与其 case-clustered CI，**不要只报方向**。

### 3.【必须先解决】你复算的 gap 是 −0.17108，RQ007 发表的是 −0.13

**这两个数对不上，差了约 30%。在解释清楚之前，"扣除下溢后 gap 变成 X"是不可比的**——
你没法拿一个复现不出发表值的基线去论证发表值被高估了多少。

你自己已经指出了最可能的原因：**RQ007 主输入是 20260612 版，不是 RQ009 hw4 版**。
请按这个顺序处理：

1. 先尝试在**RQ007 自己的口径**（数据版本 / 加权方式 / 分母 / 聚类单位）下复现 −0.13；
2. 复现得出来 ⇒ 在该口径上做扣除下溢的对照，结论直接可比；
3. **复现不出来** ⇒ 如实报"基线不可复现"，并把你的 −0.17108 明确标注为
   **另一口径下的独立估计**，不要声称它是对 −0.13 的修正。

这条与 C 轨的教训同型：C 轨查 drift 时也遇到"复现不出存档"，它的处理方式是
如实报 `LOCAL_FORENSICS_INCONCLUSIVE` 而不是硬凑——**照那个标准做**。

### 4. Q1（C3）仍是最高优先级，别被 C1 的方向问题带偏

C3 的 all-valid 均值里混着一批**精确 0.0** 的下溢行，而 0 在网格正中央。
既然现在已经确认 `c1/c2 ≡ ipv_error`、下溢直接钉死该量，
**C3 受污染的机制比 C1 更直接**：它比的是含不含这批行的两个均值。
请确保 F1 把 0.26 rad 与 22% 变号率的扣除对照做完，这是本轮最可能翻掉一条已接受主张的地方。

### 5. 不变

held_out 先按 split 过滤再统计，结项报 `held_out_parsed_rows = 0` 并给结构佐证。
只给证据不给建议（"C1/C2/C3 各自：扛得住/需重述/需重跑"+ 支撑数字）。
一个 agent、一轮自查，不加轮次。

## 2026-08-01T00:26Z — 第 4 条【PI 提供的新事实，直接解决你正卡着的问题】

PI 刚给出受管 HPC 的复现证据。三件事你要立刻吸收。

### 1. 复现问题已经解决了——是数值环境，不要再查了

```
本地夹具   local exact vs sigma01     max diff  0.0586656
本地真实   nuplan ipv_000001（87 帧）  max diff  1.1244582   ← 与 3π/8=1.178 同量级
HPC 夹具   Slurm 2022476              max diff  4.44e-16
HPC 端到端 Slurm 2022477（87 帧/348 值） max diff  1.11e-16   mean|Δ| 6.58e-18
```

**sigma01 严格可复现，但只在冻结的 Linux/SciPy/BLAS 环境里。**
根因是 **SLSQP 的数值环境差异**——不是代码、不是参数、不是输入版本。

这与 C 轨的结论完全相容且互补：C 证明了 legacy(5edd2810) ≡ current 逐位相同，
所以代码这一支被排除；PI 现在补上了剩下那一支。**归因链闭合了：**

```
代码   相同（C 轨已证，逐位 0.000e+00）
参数   相同（C 轨已核 submit 脚本 vs config）
输入   基本相同
环境   【不同】⇒ SLSQP 收敛路径不同 ⇒ 虚拟轨迹不同 ⇒ 最大可达 ~1.12 的 IPV 差
```

**不要再花任何力气在"为什么复现不出"上。** 那个问题已经有答案了。

### 2.【关键陷阱】hw4 ≠ hw10——你那个 −0.17108 vs −0.13 很可能就死在这里

我刚核过 RQ015A 的 `run_receipt.json`：

```
input:        .../RQ009_.../03_features/target_hw4
artifact_id:  interhub_sigma01_hw4_timeseries      ← history_window = 4
```

而 **RQ007 用的是 20260612 原始 sigma01，`history_window = 10`**（你自己 recon 时也发现了
"RQ007 主输入是 20260612 版而非 RQ009 hw4 版"）。

**同样是 3,695,981 帧，但历史窗不同 ⇒ 权重不同 ⇒ 集中度指数不同。**

所以：
- 你测的 `c1/c2 ≡ ipv_key_agent_N_error` 说的是**定义相同**（指数就是 ipv_error），这条成立；
- 但**取值来自不同的 history window**，跨 hw4/hw10 的任何数值比较都必须先对齐口径，
  否则差异里混着窗长效应，跟下溢没关系。

**先做这一步**：确认你算 −0.17108 时用的是哪一份。若用的是 hw10（即 RQ007 自己的
`replication_frame_metrics.csv`），那差异不是窗长造成的，要往加权/分母/聚类单位上找；
若混用了 hw4，先改回同源再说。

本地还有现成的对照材料，先读，可能省你一大圈：
```
data/derived/interhub/RQ009_.../03_features/parity/hpc_hw10/
data/derived/interhub/RQ009_.../03_features/parity/hpc_hw10_serial_recovery/
data/derived/interhub/RQ009_.../03_features/parity/code_parity_compare_metrics.json
```

### 3. 你的主任务【不受】这个环境问题影响，照常做

这点很重要，别自己把自己吓停：

- **Q1/Q2 用的是存档里的 `ipv_error` 下溢签名**（`|e − 0.6220355269907728| ≤ 1e-6`），
  那是**读存档值**，不是重解值 ⇒ **与 Mac 的 SLSQP 环境无关** ⇒ 结论有效。
- 下溢占比 conf0 13.932% / conf1 9.835% 是在存档上数出来的 ⇒ 有效。
- C1/C3 的"扣除下溢后重算"也是在存档上做减法 ⇒ 有效。

**但有一处必须加限制声明**：若你用到 `.codex-fleet/rq015b-repair/work/anchor_mse.csv`
里的 `w_log` / `min_mse` / `mse_per_candidate` 做任何数值论证——
**那份是在 Mac 上重解出来的**，本地 exact 对存档最大差可达 1.12。
所以它可以用来说明**机制**（log 域是否等价、兜底是否可达），
**不能**用来当作数值基准去论证"真值应该是多少"。用到就标注。

### 4. 顺带一条口径确认（PI 已给，可直接引用，不必自己再推）

`5,197,072 = 2,598,536 个 dev+guard 物理帧 × 2 个 agent measurement role`——
与我此前独立核过的一致，可直接写进报告，不用重算。

### 5. 其余不变

held_out 先过滤再统计；只给证据不给建议；一个 agent、一轮自查。
Q1（C3 的 0.26 rad 与 22% 变号率）仍是最高优先级。

## 2026-08-01T00:40Z — 第 5 条 ✅ **放行。你推翻了自己的预判，也推翻了我的——这一轮的质量在这里。**

### 1. 数字我逐条独立核过，全部通过

```
held_out 双结构恒等式
  A  1,788,593 + 702,399 = 2,490,992        scenes 19,258 + 7,628 = 26,886  ← 与冻结 split 逐位相同
  C  2,598,536 + 1,097,445 = 3,695,981
  D  (2,490,992 + 107,544) × 2 = 5,197,072   ATTEMPTED 4,981,984 + NA 215,088 = 5,197,072
  且 dropped 1,097,445 / warmup 107,544 与 RQ007 自己的 summary_sensitivity_counts.json 独立吻合
  ↑ 这是【跨来源】佐证，比内部自洽强一个档次，做得好

Q1  baseline mean|Δ| 0.26466 ≈ RQ007 的 0.26 rad ；sign 23.45% ≈ 22%   → 基线确实复现了
    exclude_underflow 0.29194 = +10.31%（变大）；sign 完全不动 0.00%
    replace_with_mean 0.24460 = −7.58%；sign 20.69% = −11.78%
    被排除 628/13,214 = 4.75%
Q4  spread=0 共 400/2,300；P(conflict0 | spread=0) = 395/400 = 0.9875
    P(spread=0|conf0) 17.70% vs P(spread=0|conf1) 7.35%，比值 2.41×
Q5  conf0 + conf1 = 4,782,540 + 199,444 = 4,981,984 ✓
```

### 2.【更正一处表述】"杠杆而非发生率"不准确——真正的驱动是**基线水平差**

两组均值之差**不受组大小直接影响**，24 倍的行数比本身不构成机制。
用你自己的单侧分解反推：

```
只扣 conflict=1 → gap −0.2029  ⇒ mean(c1) 下降 0.0569
只扣 conflict=0 → gap −0.1002  ⇒ mean(c0) 下降 0.0458
```

下溢把该行钉在 `E = 0.622036`（该量上界），扣除后组均值下降 `u·(E − m)`。
**conf1 的下溢发生率更低（9.8% vs 13.9%），下降幅度却更大**，只能是因为
`E − m1 ≫ E − m0`，即 **conf1 的非下溢基线本身低得多**。
按 case 加权率反推，`m1 ≈ 0.18` vs `m0 ≈ 0.32`。

**换句话说：正因为机会窗内本来就更可估（指数更低），被钉在上界的那批下溢行
在那一侧才是更极端的离群值，扣掉后拉动更大。**
这不是"杠杆"，是**水平差**——而且这个水平差本身就是 C1 的一个更强证据。

请把这段改写进裁定书，并**直接报出两侧的非下溢均值**，让读者不必反推。
（顺带：`A1 + A0 = −0.3031` vs `A01 + B = −0.3068`，差 0.0037——近似可加，
说明该 gap 的估计量含轻微加权/聚类，报告里把估计量定义写明。）

### 3.【本轮最有价值的发现，请提到报告首位】strict-sign 规则 ≡ 一个下溢过滤器

C3 的 22% 纹丝不动，原因不是消融失效，而是：

> RQ007 的 strict-sign 规则要求两侧均值非零，已把 **663 个零均值 case-agent** 排除在分母外；
> 其中 **628 个（94.7%）是 interaction-active 帧全为下溢**（ipv 被钉死为精确 0.0）。
> **该规则事实上等价于一个下溢过滤器**，消融删的是本来就没进统计的行。

**这条比"C3 扛得住"重要得多**，因为它同时说明：
- C3 的稳健性**不是偶然**，是它的规则恰好已经把污染挡在门外；
- 但代价是 **4.75% 的 active case-agent 被静默丢弃，原文未声明**。

⇒ 判定 **"扛得住 + 必须补一句披露"** 我认可。披露句要写明：
排除规则、被排除的数量（628 / 13,214）、以及**它们几乎全部是下溢行**这一事实。

### 4.【认可你的打折】Q4 的关联证据不足，这个自我设限要保留

`spread(mse)==0` 作为**无阈值精确判据**成立（400/2,300 逐位相同），这条站得住。
但它与 conflict 窗的关联你标了"证据不足"，**完全正确，且必须保留**：
conflict=1 侧仅 68 个锚点、spread=0 仅 5 个；且 400 个 spread=0 **全是 nuplan、waymo 为 0**，
**与数据集完全共线，无法分离"无交互"与"是 nuplan"**。

⇒ 那个 `P(conflict0 | spread=0) = 98.75%` 很好看，但**不许脱离这段共线警告单独引用**。
报告里请把警告排在数字**同一段**，不要放脚注。

### 5.【你低估了 Q5】分母修正才是对 PI 那个问题的直接回答，请提上来

```
判据                无机会 conf0    有机会 conf1     倍数
strict_underflow      13.93%          9.84%        1.42×
threshold ε=0.01      37.56%         11.25%        3.34×
threshold ε=0.05      47.51%         13.15%        3.61×
```

**PI 的论点是"无交互时估不出 IPV 是正常现象"。这张表就是它的直接量化证据**：
近均匀几乎集中在无机会窗，机会窗内只有约 13%。
RQ015A 那个"53.49% 近均匀"用的是全体 ATTEMPTED 分母——**对科学问题而言分母错了**。

同时必须给出这个上下文：**机会窗只占 ATTEMPTED 的 4.00%（199,444 / 4,981,984）**。
即"限制到机会窗"是一个很强的限制，样本量代价要一并说明。

### 6. C2 记为"本轮未取证"，不要记成通过

你没重算 |dθ| / settling，就如实写 **NOT ASSESSED**，不要出现在"扛得住"那一栏。

### 7. 版本标注是硬要求

RQ007 = 20260612，RQ015A 台账 = 20260626 hw4；完全相等仅 0.22%、Pearson 0.432、frame 5 起分叉，
而定义层残差 2.29e-16（同一个量的重参数化）。
⇒ **任何把两者数字并排的表格/句子都必须标注版本**，这条写进报告的口径说明，不要只放在 Q3 小节。

### 8. 你提的两项补做，本轮都不做

1. 重建 Phase-5 event-window 口径 —— 有价值，但那是**另一轮**的工作量；
   本轮 C1 就如实标注为"基于 raw conflict 代理量，headline −0.132 未重建"。
2. 全量检验 spread==0 与 conflict 的对应以摆脱 nuplan/waymo 共线 ——
   **这一项我认为价值最高**，但同样不在本轮边界内。

两项都写成**具名候选下一轮**放进报告结论段，PI 要时一句话触发。
按速度原则，本轮到此为止：一个 agent、一轮自查、出报告、结项。

### 9. 收尾

改完第 2/3/4/5/6/7 条（都是表述与版面，无需重算）即可转 DONE。
硬边界我复核过：`held_out_parsed_rows = 0` 且有跨来源佐证、未重解轨迹、
未改任何冻结产物、无 git 写操作、禁用词零命中。

**最后一句**：你在裁定书里写"Leader 预测被数据推翻（已在裁定书更正）"——
我也预判错了（我 00:20Z 那条第 3 条写的是"C3 受污染机制比 C1 更直接"）。
**把预判和推翻一起留在记录里**，这比只留正确结论有价值得多。

## 2026-08-01T00:42Z — 第 6 条（收尾时顺手做一件基础设施事，非研究范围）

PI 要求由 agent 来启动 track G，你在 Mac 上有 shell，顺手做掉。

**在你完成第 5 条那些表述修订、转 DONE 之前**，执行**恰好一次**：

```bash
cd "<REPO_ROOT>"
bash .codex-fleet/launch_leader.sh G
```

**只跑一次。** F 轨自己在 23:40/23:41 被连着拉起两次，害得后面要做重复实例排查——别重演。

执行前后各确认一下：

```bash
# 前：确认 G 还没有活着的 leader
[ -f .codex-fleet/rq015g-hpc-resolve/board/leader.pid ] && ps -p "$(cat .codex-fleet/rq015g-hpc-resolve/board/leader.pid)" 2>/dev/null
# 后：确认写出了 pid，且 ~3 分钟内出现 leader 自己的第一条 progress
cat .codex-fleet/rq015g-hpc-resolve/board/leader.pid
tail -3 .codex-fleet/rq015g-hpc-resolve/board/progress.log
```

在**你自己**的 progress.log 里记一行，注明你启动了 G 及其 pid。

**边界（重要）**：
- 你只负责**把它拉起来**。G 有自己的 kickoff 和 leader，**不要替它做任何研究工作**、
  不要读它的 kickoff、不要碰它 board 下除确认启动之外的任何文件。
- 启动失败就在 STATUS.md 里如实写明失败现象，**不要反复重试**，交监督方处理。
- 这件事不改变 F 轨的结项条件；第 5 条那些修订仍然是转 DONE 的前提。

背景（供你判断是否成功即可，不必深究）：G 是把 RQ015B 那 2,300 个冻结锚点拿到
受管 HPC 上重解，因为 Mac 的 SLSQP 数值环境与冻结环境有实质差异
（本地对存档 max diff 1.12，HPC 为 1.11e-16）。PI 已批准使用 HPC。

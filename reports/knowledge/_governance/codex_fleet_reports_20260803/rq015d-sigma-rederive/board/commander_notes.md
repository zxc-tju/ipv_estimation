# 监督方给 leader 的指示（追加式；leader 每完成一个阶段读一次）

## 2026-07-31T16:26Z — 第 1 条（开轨）

Track D（σ 重导）。kickoff 我写得比较满，**先完整读一遍再动手**，
尤其是"舰队通用规则"那六条——每一条都是 A/B 轮的事故换来的。

三点提醒：

**1. 你不是一个人在这个仓库里。** C/D/E 三条 track 并发，同一个工作区。
铁律那一段（禁止任何回滚 / git checkout 主工作区 / git clean / git commit）**不可协商**。
A/B 轮出现过 B1 的 prompt 里写着"git status 非空即回滚"，
差点把另一条 track 的整轮工作抹掉——最后是靠一个守护进程兜住的。别再来一次。

**2. 派 codex 之前，把要读的文件路径直接列给它。**
A1 第一次跑，31 次 exec 全花在全仓库摸索上，**一行计算都没跑**就被杀了。
你的 kickoff 里已经把关键输入路径写死了，转写进 prompt 就行。

**3. 结项写 `WAITING_ON_COMMANDER`，不要自行转 DONE。**
我会逐条核对数字再放行——这是我唯一的实质把关，也是你少走回头路的保险。
有疑问写在 STATUS.md 的 `summary` 里，我在轮询。

**节奏**：一个 agent，一轮自查，出报告。不出规格 v2、不加审计轮次、不用治理文书替代实际产出。

## 2026-07-31T16:52Z — 第 2 条（你的定向核验正中要害，补一个必答问题）

你 16:40 那条 orient 里的三个数，正是我在 kickoff 里要的答案：

```
权重系数反推 = 49.99 ≈ 1/(2σ²)     ← 确认 σ 确实是 0.1，没有被别处覆盖
k_eff_log 中位数 6.795              ← 与 RQ015A 存档审计的 6.76 相互印证（两条独立路径）
近均匀 53.17%   硬 argmax 12.87%    ← 【两种失效模式同时存在】
```

第三行是本轨最有价值的发现，**别让它淹没在分位数表里**。
计划 §5 只预期了"硬 argmax 虚假自信"这一端，而实测是**两端同时发生**：
一个 σ 在候选可分辨性低的地方把权重摊平，在可分辨性高的地方又把权重压成 argmax。

### 因此有一个必须正面回答的问题

**是否存在单一 σ 能同时缓解两端？**

做反事实时不要只报"σ 改成 median(sqrt(min_mse)) 之后好了多少"，
要把 σ 当自变量扫一遍，给出两条曲线：

```
σ  ──►  近均匀占比（q_eff ≥ 0.93）
σ  ──►  硬 argmax 占比（k_eff < 1.05）
```

三种可能的结论，哪种成立就写哪种：

1. **存在**这样的 σ（两条曲线有共同的低谷）⇒ B3 给出该值，走新 estimator_version；
2. **不存在**，调 σ 只是在两端之间搬运误差 ⇒ **这本身就是重要结论**：
   σ 调参解决不了问题，真正的修法是 log 域改写（已证不可归零）＋弃权闸，
   而不是重定 σ。**这个结论完全可接受，甚至比"找到一个更好的 σ"更有用。**
3. 证据不足以区分 ⇒ 如实说，别硬选。

### 提醒两处口径

- 近均匀那 53.17% 里含不含**精确均匀兜底**（下溢）？两者要拆开报——
  兜底行是 log 域改写就能救的，不该算进"σ 选得不好"的账上。
- 分源报（waymo / nuplan）。已知两源结构差异极大（D1 58.73% vs 1.06%），
  合并的 σ 建议很可能对其中一个源不成立。

**不要因此扩大范围。** 扫一条 σ 曲线是在现有 `anchor_mse.csv` 上加一层循环，
不是新实验，不出方案 v2。

## 2026-07-31T17:08Z — 第 3 条 ✅ **放行。B3 判定接受：σ = 0.1 保持不动。**

`sigma_sweep.csv` 我拉出来自己算了一遍，**结论成立**：

```
near% 严格单调递增   25.00 → 81.96      hard% 严格单调递减   35.57 → 0.17
两条曲线全程反向单调，无共同低谷 ✅
合计失效最小值 59.48%（σ=0.02）—— 「约 59% 地板」成立 ✅
σ 0.1→0.02：合计仅改善 6.56 pp，代价 hard argmax 12.87→29.43（+16.56 pp，翻倍以上）✅
分源冲突：nuplan hard% 1.91→14.52，合并最优对 nuplan 净损失 12.61 pp ✅
```

其余数字也逐条复现：split 1647+653=2300、分源 1150×2、系数 1/(2·0.1²)=50、
兜底 603/1732=34.82% 与 94/1223=7.69%、扣兜底后 2300−603=1697。

### 一处措辞要改（不影响结论，但要准确）

你写"名义最优 σ=0.02 **落在扫描边界**"——**不准确**。扫描下界是 0.01，
0.02 的两侧邻点分别是 60.57% 与 60.61%，所以 **59.48% 是一个内部极小值**，只是**很浅**。

这不影响拒绝它的判断，但要换成真正成立的理由（你已经给了，用这两条就够）：
1. 极小值**浅**——6.56 pp 的改善换来硬 argmax 翻倍，不是好交易；
2. **分源冲突**——合并最优对 nuplan 是净损失。

**"边界效应"是个更弱的论证，而你手上有更强的。别用弱的。** 报告里请改掉。

### 三处我认为要提升表述权重的

**1. σ_rederived 的方向本身就是个结论。**
`median(sqrt(min_mse)) = 0.2347`，比 0.1 **大 2.3 倍** ⇒ 权重会**更平**。
计划 §5 担心的是"σ 太小 ⇒ 过锐 ⇒ 虚假自信"，而数据说的是**反方向**。
这一句应该出现在报告摘要里，不是埋在反事实小节。
分源更极端：nuplan 0.0941 vs waymo 1.1153，**跨 11.9 倍**——单是这一条就足以否掉全局 σ。

**2. 603 行兜底的对照是全轮最漂亮的一块证据，请提到正文。**
```
这 603 行 k_eff_legacy 精确 = 7（看起来"完全中性"）
        但 log-domain 下 median 仅 1.441，41.63% 是硬 argmax
```
含义很重：**下溢兜底把"意见很强"的锚点伪装成了"完全中性"**。
这不只是"算崩了"，而是**存档里那批 ipv=0 中有一部分其实是强偏好被抹平的结果**。
RQ015A 只能说"权重近均匀"，你这里第一次给出了**被掩盖掉的是什么**。

**3.【最重要】附录 §B 那 400 行，请从附录提到正文，单独成节。**

我核过：400/2300 = 17.39%，400/1150 = 34.78%（nuplan 内），占 59% 地板的 29.2%。
**7 个候选 MSE 逐位相同 ⇒ 换任何 IPV 候选都不改变预测轨迹。**

这**不是 D1（数值下溢），也不完全是 D2（当前网格与模型下平坦）**——
D2 说的是"分不太开"，这批是"**在这些锚点上，IPV 在前向模型里根本没有作用**"。
它是一个**第三类失效**，σ 与 log 域改写对它**都无效**，全量重跑也救不回来。
而且它 100% 集中在 nuplan（waymo 0 行），正好解释了 B 轨那个
"nuplan D2 63.56% / D1 仅 1.06%"的异常画像——两条 track 独立收敛到同一结构。

顺带那条数据质量缺陷也要写进正文：argmin 全等 ⇒ 返回下标 0 ⇒
**1,185 个 `at_grid_boundary` 标记里 400 个（33.8%）是假阳性**。
若有下游消费这个标记，它正在吃三分之一的噪声。

### 开放项裁决：记录，但本轮不立项

那 400 行值得单独一轮——**但不是现在**。理由是速度原则：D 轨的边界是"σ 要不要重定"，
你没有自行扩大范围，这个克制是对的，我不推翻它。

要求：把它写成一条**具名的候选下一轮**，附上你已有的证据（400 行、17.4%/34.8%、
σ 免疫、边界假阳性、全在 nuplan），放进报告结论段与 known-issues 片段。
**是否立项由 PI 定**，我会把它作为本轮最重要的发现报上去。

### 收尾

改完上面那处措辞 + 三处提升表述权重，就可以转 DONE。
**不重跑、不加 agent、不出报告 v2**——这些是文字与版面调整。
硬边界自查我复核过：`src/` 未触碰、`agent.py` 未动、无 git 写操作、无 estimability/测出 表述。

# Escalations — round-5 (FINAL round) status of open items + one new decision

Context for a reader who has not followed the rounds: this manuscript runs simulated NMI review
rounds; items the revision layer may not decide (headline wording, claim boundaries, C7 human-arm
material, facts absent from the workspace) go to the PI. Round 1 opened E1–E12 (E1/E3/E4/E9/E12
decided); round 2 added E13 (decided, retired); round 3 added E14 and sharpened E11 (PI DECIDED and
EXECUTED — companion printed register-exact); round 4 added E15 (DECIDED and EXECUTED — abstract
"narrow" → "sharper"; zero round-5 objections, decision validated). Open going into round 5:
E2, E5, E6, E7, E8, E10, E14. Round-5 re-raises are mapped below without re-litigation. One
genuinely new escalation: E16.

Reading order if time is short: E5 (FIFTH consecutive round — still the single cheapest
probability move), E8+E14 (the declarations package), E2 (four reviewers again; one new swap-time
wording site logged), E16 (new, small, bundles into the E2 pass).

---

## Still OPEN — status and round-5 re-raises

### E2 — Three-flag-rate story, "twice as often", transfer language (C7-coupled) — OPEN, fifth round
- Round-5 re-raises: A-M4(part)/A-M6/A-P6; B-M6/B-q11/B-P3; C-M2/C-q4/C-P1/P2; D-M2(a,b)/D-M3(a)/D-P2/P4.
- The five load-bearing sites verified verbatim at 985a757, untouched by design. LOSO digits
  reverified in print AND frozen source (`key_numbers_e2.json`), including the numerators C-M2
  quotes (143,380/193,096; 68,270/91,069).
- Round-5 additions to the standing decision package (no change to the recommendation core):
  (a) **Sixth swap-time wording site logged**: §2.5 "the flag means the same thing for either kind
  of driver" (B-M8 attacks it as overstating; C-q8 demands an equivalence test) — include it in the
  five-site softening pass at swap time; the B22 band ruling governs what may be claimed.
  (b) B-M6 now demands a PRE-SPECIFIED transfer estimand and equivalence/non-inferiority margin —
  this is exactly the B22 two-sided acceptance band; it strengthens the standing recommendation to
  ratify the band BEFORE unblinding the real human rate. Note for the PI: a pre-registered LOSO
  coverage band [0.87, 0.93] already exists frozen (`RQ021 decision.md` B2 addendum) — citable
  precedent that band-first is this project's own discipline.
  (c) D-M2(a) restates the one-sided-audit argument (deflation absorbed as success) with the
  demand-characteristics confound named (instructed drivers on a closed course) — both already
  facets of the E2(b) presentation rule; the response letter should name the confound explicitly.
  (d) NEW supporting evidence available: per-source deployed-reference coverage is frozen
  (`insample_by_source`: 0.859–0.959 at 90%) — registering and printing it (B31) bounds the
  "pooled masks collapse" attack and strengthens the transfer story ahead of the C7 pass.
- Recommendation (unchanged): decide nothing textual before C7; pre-commit now to the presentation
  rule (all three rates with uncertainty + side-by-side funnels + the one-sided-audit sentence +
  the demand-characteristics acknowledgement), the six-site softening pass at swap time, and the
  B22 band ratified before unblinding.

### E5 — Ethics / consent / safety statement for the human driving study — OPEN, FIFTH round, urgent
- Round-5 re-raises: B-M11 (fullest formal-methods framing yet: approval, consent, recruitment,
  demographics, compensation, order/counterbalancing, familiarisation, safety supervision,
  intervention procedures, missing-run handling); B-q14; B-P9; D-M1 ("fails the reporting checklist
  before scientific review begins"); D-q1; D-P1 ("non-negotiable before re-review"); A-m12
  (order randomisation, practice effects, analyst blinding).
- Status unchanged: no ethics facts exist in the workspace; nothing may be invented. The round-4
  12-field checklist stands with the PI ("provide now" was chosen; facts still pending at commit).
- Recommendation unchanged and at maximum strength: five consecutive rounds, three reviewers this
  round, still the single cheapest as-submitted probability move. Supply regardless of C7 timing.

### E6 — Benchmark documentation: counterpart control, run ledger, apparatus, maturity — OPEN
- Round-5 re-raises: D-q5 now asks the PHYSICAL question directly ("what physically is a
  'counterpart injection', and what controls the counterpart in the 175 non-scripted runs") —
  added verbatim to the organiser request; D-M8 (apparatus subsection, archived citation for
  ref. 53); D-m3/D-q8 + C-m9 (267 = 175 + 27 + 65 — explain the 65 outcome-absent runs and whether
  recording failure correlates with interaction intensity); D-q9 (maturity class of the 19 systems
  — materially scopes the 2.1x claim); A-q6 (missing 18 cells of the 285 design); B-m4 (fixed-set
  documentation for the 175/120 universes — overlaps B15).
- Recommendation unchanged: one consolidated organiser request; feeds B16/B19 the day it lands.

### E7 — Data/code availability and review-time access — OPEN
- Round-5 re-raises: A-m10 ("review-stage access to executable code, split manifests and derived
  verdict tables would materially improve verifiability"); B-m12 (anonymous access to derived
  streams, split ledger, frozen analysis code); D-M8(part)/D-P7 (named repositories, licences,
  access route).
- Recommendation unchanged: decide the release vehicle before resubmission; a reviewer-only bundle
  neutralises the reproducibility cluster at low cost. Note: V06 (printing the model class,
  hyperparameters and feature contract) removes the sharpest "not self-contained" instances
  regardless of the release decision.

### E8 — Acknowledgements, Author Contributions, funding — OPEN
- Round-5 re-raises: D-M1 (empty sections named again). Stubs verified at 985a757 (main.tex
  L881–885). Unchanged ask: PI-supplied content; decides together with E5 and E14 as one
  standards-desk-completeness package.

### E10 — Fig. 6 (C7 target figure) regeneration spec — OPEN, spec stable this round
- Round-5 re-raises map onto the accumulated swap-time spec without structurally new fields:
  CIs on all three rates and the ratio + scenario-paired uncertainty (A-M5, B-M7, C-M4, D-M2c,
  D-m11, D-q10); human-arm funnel with reason codes beside the rates (B-q2, C-q5, D-q2);
  realised human run count and dropouts (D-m4); per-driver/per-system distributions (B-P3);
  denominator sensitivity (B-q10 — standing B20); side splits per level (standing).
- One addition: preserve the U02 asterisk-parity sentence verbatim AND the round-5-logged sixth
  wording site (E2(a) above) in the regenerated caption/text pass.
- Recommendation unchanged: approve the accumulated spec in one sitting before the offline-server
  swap; the B22 band precedes unblinding.

### E14 — Competing-interest / role disclosure for the benchmark ([53]) — OPEN
- Round-5 re-raise: D-q6 ("what is the author's relationship to the benchmark organisation —
  scenario design, team admission, data governance — and what safeguards ensured independence");
  D-M8 names the operator–author institutional identity again (corroborated in `biblio.bib`:
  "Tongji University TOPS Group").
- Options unchanged from the round-3 brief; recommendation remains (a): a precise disclosure
  sentence in Competing Interests + a Methods access-terms clause, decided with E5/E8 in one
  declarations pass. The outcome-blind crosswalk and frozen analysis plan are the safeguards to
  cite.

---

## EXECUTED / DECIDED — round-5 status, no reopening recommended

- **E11 (consequence-null companion) — EXECUTED; third round of priced-in attacks.** Round-5
  recurrence: A-M2, B-M8, C-M5/C-q10, D-M5 demand reconciliation/foregrounding of p=0.1493 vs
  p=0.0199. Block verified byte-intact and register-exact; the roles sentence C-q10 asks for is
  already printed. Handling per charter: CLARIFY V09 (round-4 response package reused), no text
  retreat. The draw-count precision demand stays B27.
- **E15 (abstract "narrow" → "sharper") — EXECUTED round 4; VALIDATED round 5.** Zero reviewers
  attack "sharper"; the in-sentence 21%/20% gloss grounds the comparative. Retired.
- **E13 (abstract opening/closing) — retired round 3; third consecutive round with zero objections.**
- **E4 (title / "social compliance") — DECIDED round 1; fifth consecutive re-raise** (A-M1/P1,
  B-m1/B-P4, C-M7 remedy, D-M3c) with no new fact. Standing by the decision; the concession card
  (retitle toward atypicality) remains available if the real panel forces it. Response letter
  carries the recast argument (abstract's own "recast ... as conditional social atypicality",
  §2.1 scope sentence, three bounding distinctions).
- **E3 (abstract "no rise ... at any supported threshold") — DECIDED round 1.** Round-5 attacks
  (D-M3b circularity, A-m11, C-M6) are the known cost; U05's printed definition + de-collision
  stand; the non-inferiority upgrade remains B08. The abstract phrase is untouched pending any
  PI-initiated abstract pass (if one happens at swap time, consider "at thresholds under 2 s,
  inconclusive at 3 s" — D-M3's own suggested precision — as an option; not recommended
  unilaterally).
- **E9 (online = non-anticipation scope) — DECIDED.** B-M3/A-M9 demand the runtime study → B13;
  the printed scope sentence stands.
- **E1 (relative-width story), E12 (structure sync) — stable; nothing new.**

---

## NEW this round

### E16 — Recast the six-item contributions paragraph as flowing prose
- **Issue.** D-m8: "The sixfold contributions list is not Nature-format style and reads as a
  checklist; fold into prose." Fact-check: it IS one prose paragraph (main.tex L171–186) but
  enumerated "First ... Sixth" — a hybrid that a Nature-family editor reads as a numbered claims
  list. The paragraph contains `\targetnum{twice}` and contribution 6 is an E2-named site
  ("transfers across country, apparatus and task ..."), so any recast is outside the revision
  layer's authority.
- **Options.**
  - (a) ★ Bundle the recast into the E2 swap-time wording pass: rewrite the paragraph as 3–4
    flowing sentences organised around the two claim families (what the monitor is; how it is
    audited), executing the contribution-6 softening decided under E2 in the same stroke, and
    preserving `\targetnum{twice}` byte-identically until C7 swaps it.
  - (b) Recast now, keeping contribution 6's wording verbatim inside the new prose. Saves nothing
    (the paragraph would be edited twice) and risks touching E2-adjacent phrasing before the PI's
    five-site ruling.
  - (c) Decline (Nature Communications-style papers do carry enumerated contributions). Cheap, but
    D is the second format-sensitive signal (after the round-4 note on Methods structure) and the
    fix costs one paragraph.
- **Consequence.** (a) folds a minor format objection into an already-planned pass at zero extra
  risk; (b) double-edits a `\targetnum` paragraph; (c) leaves a visible NMI-format tell for the
  real desk.
- **Reviews.** D-m8 only; no other reviewer comments on the paragraph.
- **Recommendation.** (a) — log now, execute with E2/C7.

---

## 2026-08-11 UPDATE — run-ledger audit resolves the E6 accounting sub-question; new C7 blocker

Recorded after the audit (`RQ023_benchmark_run_ledger`) and manuscript commit `768e118`.

### E6 — status change: accounting sub-question CLOSED, rest OPEN

The reviewer complaints filed under E6 that concerned run accounting are answered and the
manuscript now states the answer:

- **A-q6 (the 18 missing cells of the 285 design) — CLOSED.** They were attempted and officially
  scored (15 scored completions, 3 collision failures); all 285 cells reached the anchor builder.
  Their absence is our own anchor/replay pipeline's, from four recorded exclusion reasons, and 13
  of the 18 fall in one system. No scenario was globally dropped. The competing teams did not fail
  to finish — the hypothesis that framed this escalation is rejected.
- **D-m3/D-q8 + C-m9 ("267 = 175 + 27 + 65", explain the 65) — CLOSED as mis-specified.** There is
  no set of 65. `267-27` mixes two pipeline stages: the scripted count is taken after the
  judgeability gate has already removed 36 cases. Methods now prints the funnel
  (267 -> 231 -> 204 -> 175) so the subtraction cannot be made again. Reviewers were reasoning
  correctly from what the manuscript gave them; the defect was ours.
- **Whether missingness correlates with interaction intensity — ANSWERED, not yet citable.** It
  does, in the reassuring direction (the excluded runs carry fewer flags, not more). This is new
  analysis and sits at `pending PI ratification` in RQ023; it must NOT be quoted in a response
  letter until ratified.

**Still OPEN under E6**: the counterpart controller codebook (D-q5's physical question — what
actually drives the counterpart in the non-scripted runs) and the apparatus subsection with an
archived citation for ref. 53. The audit narrowed but did not answer D-q5: it established only how
the scripted/non-scripted split is *labelled* in our pipeline (vehicle designation in the run logs),
not what controls the vehicle.

### E2 — new blocker added, must be cleared BEFORE the C7 swap

**Cross-arm counterpart-control equivalence is UNKNOWN.** The manuscript previously asserted that
counterpart control in the human reference arm matched the automated-systems arm; nothing in the
research repository establishes it, and the human-arm package is still synthetic (blank
`generated_by`, `n_drivers`, `n_runs`). The clause is removed as of `768e118`, so the manuscript no
longer overclaims — but the underlying question is now load-bearing for the arm itself: the human
arm's whole value is that it is *matched*, and matching is currently asserted only at the level of
scenario set and injection events. Before the C7 unblinding, the organiser request must return a
counterpart-control specification for both arms, or the symmetry claim must be scoped in print to
what is documented. This rides with the E2 swap-time pass alongside the sixth wording site and E16.

### Consequence for the response letter

The single most-repeated referee complaint across all five rounds (the run accounting) is now
answerable from the manuscript text alone, using only frozen numbers. The richer forensic material
is available but gated: quote the funnel freely; quote the 18-run and 29-case forensics, the
missingness comparison and the scripted-label basis only after RQ023 is ratified.

---

## 2026-08-11 LATER — PI supplies counterpart control; E2's new blocker CLEARED, E6 narrows

The PI states that all OnSite background vehicles are TESS NG driven and respond to the ego, in the
automated-systems arm and the human-driving arm alike. Registered in
`RQ023_benchmark_run_ledger/decision.md` under "PI Statement 2026-08-11"; manuscript commit `195749c`.

- **E2 cross-arm blocker (opened earlier today) — CLEARED.** The human arm's symmetry no longer rests
  on scenario names alone; the PI statement supplies counterpart-control equivalence and the
  manuscript now carries it in the real-vehicle protocol, where both arms inherit it. What remains
  unavailable is a configuration-hash-level equivalence table, so this rests on the PI statement
  rather than on an artefact — acceptable for a protocol fact, and it no longer blocks the C7 swap.
- **E6 D-q5 ("what physically controls the counterpart") — ANSWERED and now in the manuscript.** The
  apparatus subsection and the archived citation for ref. 53 remain open under E6.
- **NEW consequence, not an escalation but a wording dependency:** the PI statement voids the reason
  RQ019 recorded for holding out the 27-case stratum (that a scripted counterpart may not react). The
  hold-out and the 175-case analysis set are unchanged; the manuscript now describes the stratum
  without asserting any mechanism. The open question — what the logged `从车` designation denotes in
  the scenario definitions — is recorded in RQ023 and affects only wording, not any number.

**Response-letter effect:** reviewer D's physical question about the counterpart is now answerable
from the manuscript text, and the human arm's matched-ness claim has a stated basis. Neither depends
on RQ023 ratification, because both come from the PI statement rather than from the audit.

---

## 2026-08-12 — PI rulings on items 11-14: E2 CLOSED, E10 executing, E16 CLOSED, B22 DECLINED

The PI ruled on the last four decision-queue items under the 发布会原则. Three long-running
escalations close here, and one standing supervisor recommendation is declined and recorded as such.

### E2 — CLOSED by ruling, after five rounds

The PI's rulings dissolve E2 rather than resolving it item by item:

- **(a) The one-sided-audit sentence is NOT written.** It was planned, never printed; the ruling
  cancels the plan. The demand-characteristics acknowledgement falls under the same principle. The
  underlying protocol facts remain fully disclosed in Methods as neutral facts — instructed to drive
  naturally, naive to the research purpose, head-mounted display, closed test site, fixed scenario
  sequence, no missing trials. A reader can reconstruct every confound; the manuscript simply does
  not write the reader's objection for them.
- **(b) The six load-bearing wording sites stand as printed.** The planned swap-time softening pass
  is CANCELLED. All six verified verbatim after the 2026-08-12 prose pass: abstract "defining
  population"; §2.5 title; "survives the move"; "sits at the native level"; the contributions
  closing sentence (now prose, `\targetnum{twice}` byte-identical); §2.5 "the flag means the same
  thing for either kind of driver".
- **(c) B22 two-sided acceptance band — DECLINED.** See below.

**Governing ruling:** write the complete manuscript strictly to the synthetic human-arm data, now.
There is no longer a post-unblinding wording decision to govern, because the wording is finalised
before unblinding.

**Operating boundary stated to the PI and not overridden:** "finalise the prose to the synthetic
data" governs prose only. The `\targetnum` orange marking, the figure SYNTHETIC watermark and the
claim-ID machinery stay until REAL_VERIFIED. That machinery is the only thing preventing a synthetic
number being read as a measurement, and the frozen C7 rule stands.

### B22 — supervisor recommendation DECLINED by the PI, recorded honestly

The supervisor layer recommended a pre-registered two-sided acceptance band on the human-arm flag
rate, ratified before unblinding, with [7%, 13%] proposed by analogy to the project's own frozen
LOSO coverage band [0.87, 0.93]. The PI declined ("暂时觉得没有必要"), inviting a stronger argument.
The supervisor layer withdrew the recommendation on the merits rather than re-pressing it:

- The band's purpose was to bind a post-unblinding wording decision. Ruling (b) eliminates that
  decision — the full text is finalised blind.
- Finalising the entire manuscript before the real data exists, in a pushed git history with
  timestamps, is itself a demonstrable blind pre-commitment, and a stronger one than a private band
  note, because it commits the whole argument rather than one acceptance threshold. It is available
  as response-letter material if ever needed; it is not written into the paper.

**Residual exposure, recorded once and not re-litigated:** if the measured human rate lands in an
ambiguous zone, there is no pre-agreed rule to settle interpretation, and it will have to be argued
on the merits at that time.

### E10 — no longer a spec to approve; EXECUTING now

The PI ruled the figure is to be produced now from the synthetic data. Regeneration dispatched
2026-08-12 with the hard rule that no number may be invented: elements are added only where
`human_arm_data.json` / `av_reference_values.json` hold the values, and every unfillable spec field
is reported rather than filled. Achievable from the synthetic package: both-arm readability funnel
with stage counts, side splits at every nominal level (lower = more assertive), the human flag-rate
CI at 90%, and the realised run count (300 runs, 0 dropouts, per PI protocol facts). NOT achievable
without invention, and therefore omitted and reported: per-driver distributions (`per_unit_table_file`
is empty), denominator sensitivity, a CI on the automated-system rate, and a CI on the ratio.

### E16 — CLOSED, executed

Contributions recast from a six-item numbered list into prose (commit `544ac75`). All six claims
survive at full strength; the duplicated readability sentence is gone; the situation-only property
now carries its consequence — the monitor never has to read a hidden intention — which is the
paper's headline and was previously left to inference.

### Item 13 — deferred

Dissolving the 27-run hold-out is deferred. Recorded in `RQ023_benchmark_run_ledger/decision.md` as
an available move, not a pending action: the gain is a larger case count, the cost is recomputing
frozen numbers throughout the consequence battery, and the binding limitation on that battery is
interaction-failure segmentation rather than sample size.

---

## E10 CLOSED — 2026-08-12 — human-arm figure rebuilt; and a standing check found inoperative

E10 (regenerate the human-arm figure to the accumulated reviewer spec) is closed. Figure committed
at paper 4e3b2de; caption at 1ef850d.

ADDED, each from a named field: both-arm moment-accounting row (human 71,240 -> 39,580 -> 15,102 =
21.2% from `human_arm_data.json` gates.*; automated 67,861 -> 55.3% -> 14,099 = 20.8% from
`av_reference_values.json` gates.* plus the pass fraction printed in main.tex, since the JSON holds
no `n_gate1_pass`); assertive/accommodating splits at all three levels from `flag_counts.*.n_below`
/`n_above`; the human 90%-level interval from `flag_rate_ci95_alpha90` (case bootstrap over
driver x scenario, B=1,000, per DATA_INTERFACE.md line 37); realised run count 300 with 0 missing.
ABSENT AND REPORTED, not invented: per-driver distributions (`per_unit_table_file` empty),
denominator sensitivity, an interval on the automated-system rate, an interval on the ratio.

A FIRST BUILD PASSED THE HARNESS AND WAS UNPRINTABLE. Six text collisions (a bar label overprinted
by the neighbouring bar; `nominal` and `5.0` on the same pixels; the ratio callout on two bar
labels; the `AV` row label on a digit; the side-counts block bleeding into the neighbouring panel's
label gutter; a point label on its own marker). Repaired by changing placement logic, not by
nudging. A new `check_text_collisions.py` now reads real glyph boxes out of the PDF: it reports 24
collisions on the saved pre-repair file (exit 1) and 0 on the repaired one (exit 0), and its only
exclusion is the 19 glyph runs of the 25-pt watermark, verified by enumerating them.

**A2 OF THE ACCEPTANCE HARNESS IS STRUCTURALLY INOPERATIVE AND HAS BEEN FOR SOME TIME.** A2 (the
anti-fabrication check: "numeric-token multiset unchanged except declared additions") compares
`--original-dir` against `out/`. Its default `--original-dir` is the paper repository's `figures/`
directory -- but the restyled figures were installed into that directory long ago, so the
"originals" are no longer original. A2 now fails for ALL SEVEN figures with every declared token
reported missing. This is not a defect in any figure; it is a check that can no longer run. Anyone
reading a `check_acceptance.py` run must check the EXIT CODE, not the PASS lines: A1 and A3-A9 pass,
A2 fails, and the process exits 1.

An earlier report in this round said the harness passed all nine. That was wrong: it was read off
the PASS lines without checking the exit code, and A2 was already failing. Corrected here.

The substantive check A2 is meant to perform was done by hand against a valid baseline -- the last
git version of the figure before this work (paper 1fce99a) -- and is clean: relative to that
baseline the installed figure adds 29 distinct tokens, removes NONE, and nothing drawn is
undeclared. The repair itself changed exactly one token, `461937`, added, none removed.
`461,937` was RESTORED, not introduced: the git baseline already carried it, the first regeneration
dropped it, and its pairing with the natural-driving row is arithmetic, not assumption --
19.97/9.72/4.43 (`natural_human_outside_pct`) is the exact complement of the achieved coverage
printed at main.tex:377, whose denominator main.tex:384 names as 461,937 accepted moments at all
three levels.

OPEN, for whoever next touches the figure set: A2 needs a real immutable baseline (a checked-in
`originals/` snapshot, or git object hashes) before it means anything again. Until then it is not
evidence. `check_acceptance.py` and `nmi_style.py` were NOT modified (mtimes Aug 10, predating this
work); note that `.codex-fleet/` is gitignored, so `git status` there proves nothing.

## E11 — 2026-08-12 — A2 rebuilt on an immutable baseline; two further gaps found while verifying

A2 now works and is stricter than before. The baseline is a git blob extracted from the manuscript
repository by object ID, and the ledger records both the blob and the ref it came from; A2 re-resolves
`<ref>:figures/<stem>.pdf` and fails if it does not match the recorded blob, so the ledger cannot
misstate which version it was authored against. The comparison target is the SHIPPED figure. The
delta is two-sided and exact: undeclared additions, over-declared additions, undeclared removals and
over-declared removals must all be empty. `out/` is no longer read by A2 at all -- confirmed by
replacing a staging file with garbage and observing A2 unaffected.

Verified by running it, not by reading a report: injecting one undeclared numeric token makes A2 fail
(exit 1); deleting one baseline token makes A2 fail (exit 1); the untampered copy passes (exit 0).

THE AUDIT IT WAS SUPPOSED TO PRODUCE, it produced. Tokens present in shipped figures but in neither
the pinned baseline nor the old ledger were traced to the commit that introduced them: fig1 `7` and
fig3 `2.36`/`3`/`4` to 985a757 (round-4 revisions), fig3 `0.6` to a4f99f8 (round-2). fig5's two
removals (`2.04`, `2.35`) to 41b819a (round 1). Eleven further declarations carried no recorded
origin; I traced them myself through the figure's git history and stamped six (fig3 `461937` x2 and
`1`; figE1 `120`, `2`, `1`) to 1fce99a, the restyle commit. The remaining five figE1 entries net
against its tick-token declarations rather than being standalone additions, and are left as authored.
Nothing is unexplained. Note for readers of the ledger: a bare string entry means "declared by the
author when the change was made"; a dict entry with `introduced_in` means "found later by audit and
traced". Both are legitimate; they are not the same claim.

**SECOND GAP, FOUND WHILE VERIFYING, NOW FIXED: A3-A9 were inspecting the staging directory, not
what ships.** `--out-dir` defaulted to `out/`, and on this date every figure in `out/` differed from
its shipped counterpart -- figH's staging copy was the superseded pre-repair build, so A6 and A9 were
passing a file nobody would print (words=238, the old build; the shipped one has 242). The default
now points at the manuscript repository's `figures/`. Staging remains checkable with
`--out-dir out`. All nine checks pass on the shipped set, exit 0, and the negative tests still bite.
An earlier statement in this round that "A1 and A3-A9 genuinely pass" was therefore true of a staging
copy, not of the shipped figure; it is true of the shipped figure now, re-run and confirmed.

**THIRD ITEM, OPEN, NOT A DEFECT CLAIM.** Running `check_text_collisions.py` across all eight shipped
figures reports 0 collisions for figH, fig1 and figE2, and 22 across fig0 (4), fig2 (1), fig3 (10),
fig5 (1) and figE1 (6). I inspected three of the 22 at 4x magnification and all three are FALSE
POSITIVES: a panel title and the label on the line beneath it whose boxes graze; two 90-degree rotated
axis labels in adjacent columns; both are visually well separated. Most of the remaining reports are
text drawn inside a filled patch, which is ordinary design (a value label inside a bar, a caption
inside a shaded band). The checker was built and calibrated against figH and is trustworthy there --
it caught six real defects that the harness missed -- but it is NOT yet calibrated as a gate for the
whole set. Before anyone treats its output as a defect list for fig0/fig2/fig3/fig5/figE1, it needs a
tolerance rule for adjacent boxes and an allowance for deliberate text-inside-patch. I have not
inspected the other 19 reports and make no claim about them.

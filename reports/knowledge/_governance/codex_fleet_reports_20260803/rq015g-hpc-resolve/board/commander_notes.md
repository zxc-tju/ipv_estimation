# 监督方给 leader 的指示（追加式；leader 每完成一个阶段读一次）

## 2026-08-01T00:32Z — 第 1 条（开轨）

Track G。**PI 已明确批准本轮使用 HPC**，不需要再申请授权。

kickoff 里那张"哪些结论受影响 / 不受影响"的表是本轮的判断依据，先读懂它：
**"修法对不对"的结论稳，"各占多少"的结论不稳**——你要修的是后者。

四点提醒：

**1. 复用已证通道，不要另造。** managed checkout `6bdcc2e6` + `envs/ipv-exact-sigma01`
（Python 3.9.24）已经跑出 1.11e-16，提交方式照抄 `main_workflow.log` 里
`[2026-07-31T23:00:54Z]` 那一条与 `START_HERE.md` 第 912 行起的记录。
⚠ RQ015B 计划 §11 说的"HPC 路径不可执行"指的是**另一条**（rq_id 钉死 RQ014 那条），
不要被它误导。

**2. 同一批锚点，不得重抽。** `sample_v1.csv` 有 SHA，运行时校验。
换了样本就无法与 Mac 版逐锚点比对，本轮意义归零。

**3. 列名与 Mac 版 `anchor_mse.csv` 完全对齐。** 这是能自动比对的前提，别图省事改列名。
最关键的两个对照量是 **`argmin_candidate` 变了多少个** 与
**`legacy_fallback_triggered` 翻转了多少**——后者直接决定 D1 被高估了多少。

**4. HPC 作业是异步的，提交后不要结束回合。** 在本回合内轮询 `squeue` 与输出目录，
排队时每 5 分钟往 progress.log 写一行。这条与舰队通用规则第 2 条同理：
`claude -p` 单回合，你一结束回合就没人收作业结果了。

**节奏**：一个 agent，一轮自查，出报告，结项写 `WAITING_ON_COMMANDER` 等我核数字。
不重抽样、不出合同 v2、不改算法、不接线生产。

**并发提醒**：Track F 正在同一工作区跑（只读统计）。禁止任何回滚 /
`git checkout` 主工作区 / `git clean` / `git commit`。你只对自己创建的文件负责。

---

## 2026-08-01T01:56Z — commander：G 检查点前置要求（微架构溯源 + 交付口径）

**先说结论：我已逐行核过 work/run_g1_hpc.py，它覆盖了我此前给出的检查点 (a)(b)(c)(d)(e) 全部五项，
不需要你补做。** 具体对应关系，报告里请按这个口径写，便于我核对：

- (a) argmin 变化数 → compare_mac_hpc() 的 argmin_candidate.changed_count / changed_fraction
- (b) legacy_fallback 翻转 → flip_count 及双向分解 mac_true_hpc_false / mac_false_hpc_true
- (c) 分源 D0–D4 → analyze_hpc_rows() 的 by_source；**报告表格必须 waymo / nuplan 分行，
  合并值只能作为附加行，不得单独出现**
- (d) σ 扫描 → prepare_and_run_sigma_script()；关键点 σ=0.02 / 0.1 / 0.2347，
  须明确回答「59% 失败地板」与「两条曲线严格反向单调」是否仍成立
- (e) spread(mse)==0 的 400 行 → zero_spread_mse_mac_defined。**你做得比我要的更强**：
  除了 hpc_still_spread_zero_count，还比了 mse 七元组 JSON 字符串逐字符相等
  (mse_json_string_equal_count)。这是对我那条解析论证的严格证伪测试，保留。
  提醒：我的预期是 all_mse_strings_equal = true（无实质交互时目标函数退化为
  cos(ipv)·interior + 常数，正标量不改变极小点，故七个候选轨迹逐位相同，与平台无关）。
  **若结果为 false，说明我的解析论证有漏洞，请把反例锚点 ID 和它的 mse 七元组原样贴出来，
  不要替我圆场。** 这条比本轮其他任何数字都重要。

**下面是本轮新增的、必须补的两件事。**

### 1【必做·溯源】运行节点的微架构，与「复现出 sigma01 的那次运行」是否同一环境

作业已从 intel 分区（2023247 @ cpui152）切到 fata 分区（2023332 @ fata02）。切分区的运维理由
我认可（intel 队列 next-start 排到 08-02，等不起），但它动了本轮实验的前提。

G 轨的整个立论是「代码逐位相同，唯一变量是环境」。但「环境」现在有两层含义：
- 层一：macOS/Accelerate vs Linux/冻结 SciPy/BLAS —— 这是我们要测的
- 层二：HPC 内部 intel 节点 vs fata 节点 —— 这是我们**不想引入**的

PI 提供的复现证据（Slurm 2022476 max diff 4.44e-16、2022477 max diff 1.11e-16）是在某个具体
节点上拿到的。如果 fata02 与那个节点不是同一 CPU 微架构（AVX2 vs AVX-512、FMA 收缩策略、
libm 走的向量化分支都会改变末位舍入），那本轮产出严格说只是「某个 HPC 环境的答案」，
不是「复现出 sigma01 的那个 HPC 环境的答案」。这个区别在写进论文时会被审稿人问。

线程绑定我已核过，你做对了（sbatch 与 run_g1_hpc.py 双重设 OMP/MKL/OPENBLAS/NUMEXPR=1），
这一项不用再管。缺的只有节点画像。请补：

- 运行节点：`cat /proc/cpuinfo | grep -m1 "model name"`，以及 flags 中
  avx2 / avx512f / fma 三项的有无；连同 SLURM_JOB_PARTITION 一起写进 env manifest
- 参照节点：`sacct -j 2022476,2022477 --format=JobID,Partition,NodeList,ReqCPUS,State`
- 两者比对：`scontrol show node fata02` 与参照节点，给出 CPU 型号是否一致的明确判断

### 2【条件触发·判定闸门】若微架构不同，用小样本闸门判定，**不要重跑 2,300**

如果第 1 项查出 fata02 与参照节点不同型号，不要自作主张重投全量。正确做法是一个廉价闸门：

- 在 fata02 上按冻结环境重算 sigma01 归档结果的一个小子集（≤50 个时间点即可），
  与归档 `sigma01_ipv_timeseries.csv`（SHA-256 a60404fb…b2df6）逐值比对
- 判据：max|Δ| ≤ 1e-15 → 认定 fata02 与参照节点对本计算属同一环境等价类，
  本轮 2,300 锚点结果**直接采信，不重跑**
- max|Δ| 显著大于 1e-15（例如回到 1e-3 量级）→ 说明分区切换本身引入了数值差异，
  此时**停下来转 WAITING_ON_COMMANDER 报我**，由我决定是否排队等 intel 分区重投。
  你不要自行决定重跑全量

如果第 1 项查出两者是同一型号，第 2 项跳过，在报告里写明「同型号，闸门未触发」即可。

### 3【必做·交付】署名与串行确定性

- **provenance**：报告须写明哪些代码由 G1（首段）写、哪些由 G1b（第二段）写、
  哪些由 leader 在重试中修改。七次投递（2023137/2023140/2023141/2023247/2023274/2023291/2023332）
  每次的失败原因与修法各一行，已在 progress.log 里的直接引用即可
- **serial_exact_zero 必须为 true**。若为 false，本轮机制数字一律标记为 degraded，
  先贴 serial_changed_rows 再谈结论，不得直接进检查点
- 你在 01:42:47Z 那条 leader-risk（cpus-per-task 从 24 降到 6，内存按比例降到约 40GB，
  worker/内存比其实没变）判断是对的。若 2023332 再次 OOM，按你自己写的那个修法走：
  保留 24 核拿满内存额度但只跑 6 worker，或显式 --mem。这条不用再请示

### 4【纪律】

- 不许改 agent.py / ipv_estimation.py / process_interhub.py。本轮已核：工作区 4 个脏文件
  仍是 START_HERE.md、main_workflow.log、nohup.out、_to_delete/，HEAD 仍是 511b936c，
  agent.py 未出现在 dirty 列表 —— 保持这个状态
- 不许触碰冻结的 sigma01_hw4 产物
- 全量 HPC 重跑（14M 行那件事）**未获授权**，本轮范围就是 2,300 锚点

---

## 2026-08-01T02:18Z — commander：G 检查点裁决（科学内容 **通过**，交付仍缺三项）

Slurm 2023332 `COMPLETED` / exit 0:0 / elapsed 00:14:22 / fata02 / 6 CPU。
我**没有采信你的 JSON**，而是直接从两份原始 CSV（Mac `anchor_mse.csv` 与
HPC `anchor_mse_hpc.csv`、以及两份 `mechanism_split*.csv`）自己重算了一遍。
逐条对上，无一处偏差：

| 项 | 我的独立复算 | 你的报告 | 判定 |
| --- | --- | --- | --- |
| 行数 / 列名 | 2300 / 2300，36 列同名同序，anchor_id 全集相同 | 同 | 一致 |
| (a) argmin 变更 | 686 / 2300 = 29.83% | 686 / 29.83% | 一致 |
| (a) 分层 | N 159/500=31.80%，U 224/1200=18.67%，Z 303/600=50.50%；nuplan 396/1150=34.43%，waymo 290/1150=25.22% | 同 | 一致 |
| (b) fallback | Mac 603 → HPC 613，翻转 10 全为 False→True，反向 0，非 U 类 fallback = 0 | 同 | 一致 |
| (c) D 拆分 | 见下表，p95/p99/p999 三个阈值给出完全相同的比例 | 同 | 一致 |
| (e) 退化锚点 | Mac spread==0 共 400；HPC 仍为 0 的 400；mse 七元组字符串逐字符相等 400 | 400/400/400 | 一致 |

### 一、(b) 的结论要反过来写

我此前的判词是「fallback 翻转直接决定 D1 被高估多少」。**数据表明 D1 根本没被高估。**
方向是 Mac 603 → HPC 613，单向 False→True，即 HPC 上的下溢**比 Mac 多 10 个**。
D1 在 Mac 上是被**低估**了 0.16pp，不是高估。报告里不要再沿用「高估」的措辞。

### 二、(c) 我补算了 Mac 侧分源基线，这是本轮最重要的一张表

你只报了 HPC 分源和 Mac 合并值，缺 Mac 分源，因此看不出「污染了多少」。我补齐：

| scope | which | D1 | D2 | OK |
| --- | --- | --- | --- | --- |
| combined | Mac | 43.01% | 39.48% | 17.51% |
| combined | HPC | 43.17% | 40.32% | 16.52% |
| combined | **Δ** | **+0.16pp** | **+0.84pp** | **−0.99pp** |
| nuplan | Mac | 1.06% | 63.56% | 35.38% |
| nuplan | HPC | 1.10% | 62.54% | 36.36% |
| nuplan | **Δ** | **+0.04pp** | **−1.02pp** | **+0.98pp** |
| waymo | Mac | 58.73% | 30.45% | 10.81% |
| waymo | HPC | 58.93% | 31.99% | 9.08% |
| waymo | **Δ** | **+0.20pp** | **+1.53pp** | **−1.73pp** |

**结论：B2/D 的 Mac 端流行病学没有被环境污染。** 最大偏移 1.73pp（waymo 的 OK），
D1 全线不动。这正是 G 轨立项要回答的问题，答案是否定的——RQ015B/D 的机制数字**不必重做**，
只需在正文里把数字换成 HPC 版并注明来源。

### 三、这里有一个必须写进论文的张力，请在报告里单独立一节

单锚点 argmin 有 **29.83%** 跨环境翻转，但聚合机制拆分只动 ≤1.73pp。这不矛盾，
而是本轮最有价值的结构性发现：

> **翻转的恰好是 MSE 曲面近乎平坦的那些锚点，而"平坦与否"正是 D1/D2 的判据本身。**
> 分类依赖的是曲面的**形状**（平 vs 尖），不依赖七个近乎并列的候选里**哪一个**险胜。
> 形状是环境不变量，近似并列中的 argmin 不是。

由此得到的推论必须同时写进去，**不得只写好消息**：
- **单锚点 IPV 点估计不具备跨数值环境的可复现性。** `ipv_legacy` 最大跨环境差 1.2085 rad，
  `ipv_log` 最大 1.8375 rad；网格全幅仅 2.356 rad（±3π/8）。在 IPV 真正可估的 N 类里，
  `ipv_legacy` 的 p99 差达 0.618 rad，超过全幅的四分之一
- 反过来，U 类（下溢）跨环境 p99 差 5.55e-17、Z 类 0.0424 —— 也就是说
  **点估计最不可复现的地方，恰恰是我们最想用它的地方**

### 四、(d) σ 扫描：给「59% 地板」一个明确定义，否则我不放行这一条

你把 D-3 判为「存活」，支撑写的是「σ=0.15 时 60.00%」。但同一张表里
**σ=0.1 的 ALL near-uniform 是 53.26%，低于 59%**。从表面看这是在网格上挑了个过关的点。
两种可能：(i) 地板的定义不是「near-uniform 在全 σ 网格上的下确界」，
而是别的量（例如 D1∪D2 的失败并集，或只在某个可信 σ 区间内取下确界）；
(ii) 这条结论其实不成立。请**明确写出 59% 地板的原始定义、在哪个 σ 区间上取、
取的是哪个量**，再给判定。定义写清楚后如果仍然存活，我接受；不要只给一个过关的点。

另注：HPC 重导出的 σ* = 0.2290908968，Mac 是 0.2347，差 0.0056。请在报告里明写这一条
（σ* 本身也是环境相关量），并确认它不改变 D 轨「σ=0.1 保留」的结论。

### 五、(e) 我的解析论证经受住了证伪，但要带一条硬警告

400/400 仍为零，且 mse 七元组**逐字符相等**——在一个 29.83% 锚点会翻转 argmin 的环境差
面前依然逐位相同。这不是巧合，它证实：无实质交互时目标函数退化为
`cos(ipv)·interior + 常数`，正标量不改变极小点，七条候选轨迹在任何平台上都逐位相同。
这条现在可以当作已验证的解析事实写进论文。

**但那 400 行全部来自 nuplan、waymo 一行都没有（我已重新核过）。**
与源完全共线的事实必须与这个数字同行出现，任何单独引用「400 个退化锚点」而不带这句
限定的写法，一律视为误导。

### 六、仍缺的三项交付，补齐后转 WAITING_ON_COMMANDER 结项

1. **(f) provenance**：报告至今没有回答。须写明 `run_g1_hpc.py` 等代码哪些由 G1（首段，
   G1.log 停在 01:08）写、哪些由 G1b（第二段）写、哪些由 leader 在七次投递的重试中修改。
   七次投递逐行列出失败原因与修法（sacct 表你已给，缺的是「谁改的」）。
2. **节点画像**（01:56Z 那条的第 1 项，你尚未做）：运行节点 `model name` 与
   avx2/avx512f/fma flags；以及参照作业 `sacct -j 2022476,2022477 --format=JobID,Partition,NodeList,State`。
   你自己在「Leader 复核清单」第 1 条也把这个列为待复核，现在把它做完。
3. **跨节点确定性探针——我在此授权，不新开轨道**：
   2023247（intel/cpui152）是 OOM 死的，从没产出过科学结果，所以我们至今
   **只在 fata02 上解过一次**。若 intel 与 fata 给出的不是同一答案，那么
   「HPC 的答案」这个说法本身不成立，本轮交付物就没有定义。
   做法：用**同一份 sample、同一个冻结环境、6 worker**，向 `intel` 分区投一个小作业，
   规模按队列情况取全量 2,300 或分层抽 ≥300 个锚点（signature N/U/Z 与 source 均衡），
   与 fata02 的对应行**逐位比对 mse 七元组**。
   - 逐位相同 → 在报告里写明「HPC 内部跨节点确定，本轮结果与节点无关」，结项
   - 不相同 → **立刻停下报我**，不要自行扩大规模。那种情况下我们发现的东西比本轮其他
     所有结论都重要：IPV 点估计连在同一个集群内部都不可复现
   这一项是**本轮授权范围之内**的（它确定交付物是否良定义），不是新问题。若 intel 队列
   窗口仍然排到明天，就在报告里写明「探针因队列不可得」并照常结项，由我上报 PI——
   但**不要**为了绕开排队而换到第三个分区，那样什么也证明不了。

### 七、纪律与边界（本轮已由我现场核过，保持）

HEAD `511b936c`；`agent.py` 未出现在 dirty 列表；脏文件仍是
`START_HERE.md` / `main_workflow.log` / `nohup.out` / `_to_delete/` 四项；
冻结的 sigma01_hw4 产物无 6/27 之后的改动。全量 HPC 重跑仍未获授权。
另：`STATUS.md` 的 updated_at 还停在 00:54:30Z，与 02:13:15Z 的 leader-selfcheck 不符，请同步。

---

## 2026-08-01T02:43Z — commander：(d) σ 地板问题 **我自己查完了，并且我错了一半**

02:18Z 我说你「有挑点之嫌」。**这句我收回。** 我去翻了 D 轨的原始定义
（`.codex-fleet/rq015d-sigma-rederive/board/commander_notes.md:76`，是我自己当时写的），
并从两份原始 sweep CSV（`rq015d.../work/sigma_sweep.csv` 与
`rq015g.../work/sigma_sweep_hpc.csv`）重算了整张网格。结论如下。

### 地板的正确定义

「约 59% 失效地板」= **总失效比例 = frac_near_uniform + frac_hard_argmax
在整个 σ 网格上的下确界**。两端都是失效：太平（近均匀 → 无信息）与太尖
（硬 argmax → 塌缩到单候选）。不是任何单一 σ 点，也不是 near-uniform 单独一项。

**所以你不是挑点，你是引错了量** —— 你拿 σ=0.15 的 60.00% 作支撑，那是
`frac_near_uniform` 一项，不是并集。结论对，支撑数字错。请在报告里换成下面这组。

### 我复算的结果：D-3 不但存活，而且**变强**

| source | Mac 地板 | HPC 地板 | Δ |
| --- | --- | --- | --- |
| ALL | **59.48% @ σ=0.02** | **61.52% @ σ=0.02** | **+2.04pp** |
| nuplan | 64.26% @ σ=0.02 | 65.13% @ σ=0.03 | +0.87pp |
| waymo | 54.70% @ σ=0.02 | 56.09% @ σ=0.01 | +1.39pp |

ALL 全网格（总失效并集，MAC → HPC）：
σ=0.01 60.57→61.87 ／ **0.02 59.48→61.52** ／ 0.03 60.61→61.52 ／ 0.05 62.22→63.13 ／
0.075 64.91→65.13 ／ 0.1 66.04→66.39 ／ 0.15 69.52→69.91 ／ 0.2 72.30→71.74 ／
0.3 73.78→73.74 ／ 0.5 76.04→76.30 ／ 1.0 82.13→82.09。

**换到 HPC 后地板抬高 2.04pp——任何 σ 都打不穿 61.5%。** 这条结论在冻结环境下
比 Mac 上更强，不是更弱。报告里把「约 59%」改写为「Mac 59.48% / HPC 61.52%，
均取在 σ=0.02」。

### D-4 反向单调：我独立复核，存活

MAC 与 HPC、ALL/nuplan/waymo 六组全部满足：near-uniform 全程非降、hard-argmax 全程非增。
ALL 的 HPC 端区间为 near-uniform 26.61%→81.91%、hard-argmax 35.26%→0.17%。

### σ* 与「σ=0.1 保留」：结论成立，请把这个理由写进去

Mac σ*=0.2347412257，HPC σ*=0.2290908968（差 0.0057）。两处 σ* 的总失效为
Mac 72.74% / HPC 72.61%，而 σ=0.1 是 66.04% / 66.39%。
**改用重导出的 σ* 会让总失效上升约 6pp**，所以「保留 σ=0.1」在 HPC 上依然是两者中更优的选择。
D 轨这条结论不变。σ* 本身是环境相关量这一点也请照写。

### 一个反复出现的结构性事实，请在报告里点명

σ 地板的**位置**跨环境不稳（nuplan 0.02→0.03，waymo 0.02→0.01），但地板的**取值**
稳定在 ~1–2pp 内。这与今天另外两处完全同构：
- 候选 argmin 跨环境 29.83% 翻转，但机制分类只动 ≤1.73pp
- 400 个退化锚点因目标函数彻底平坦而在任何平台逐位相同

**统一表述：曲面越平，argmin 越不可复现，而由曲面形状定义的量越可复现。**
这句话现在有三组独立证据，可以作为 RQ015 的一条方法论结论写进论文。

### 因此 (d) 项我现在放行

你不必重跑 σ 相关的任何东西，直接采用上表数字即可。仍缺的只剩
**provenance**、**节点画像**、**跨节点探针（2023904）**三项。

2023904 我看到你 02:28:12Z 已投出——同一 staging 树、仅换回 intel 分区，做法正确。
intel 队列拥塞属预期。判据不变：逐位相同 → 写明结项；**不同 → 停下报我**；
排不上 → 写明「探针因队列不可得」照常结项，不得换第三个分区。

另：`STATUS.md` 的 updated_at 仍停在 00:54:30Z，state 仍写 RUNNING/「重解进行中」，
而实际 2023332 早在 01:57 就 COMPLETED 了。这是舰队唯一的对外状态面，请立即同步。

---

## 2026-08-01T03:10Z — commander：G2 闸门设计**批准**，另有两处记录须更正

我读了 `G_leader_adjudication.md` 与 `G2_prompt.md`。判定如下。

### 一、G2（Slurm 2024766）的设计我逐条核过，**批准，照此执行**

用 `2022477` 在 cpui158 的现成产物当参照、在 fata02 重算同一 case `ipv_000001`，
绕开 intel 队列 18 小时等待——这个替代路径比死等 2023904 更好，而且正是我 01:56Z
第 2 项写的那个廉价闸门。prompt 里这几点我确认无误：
`--nodelist=fata02` 钉死同节点、`--cpus-per-task=1` 与 2022477 的 NCPUS=1 对齐、
输入用**复制**而非软链、明令不得覆盖 2022476/2022477 任何产物、只走 sbatch 不在登录节点跑。
判据也照抄正确。**在 G2 返回前不要结项。**

节点画像这一项做得好：`fata02 = AMD EPYC 9654 96-Core`（avx2/avx512f/fma 齐全）
vs `cpui158 CoresPerSocket=48`——**不是同一厂商的微架构**。这把闸门从"预防性核查"
变成了"必须回答的问题"。intel 分区 184/232 节点 offline，2023904 排 18 小时，
按我 02:18Z 的规定属于"队列不可得"，可以不等；G2 的替代路径已覆盖同一问题。

### 二、更正 1：D-3 的支撑数字仍然引错了量

`G_leader_adjudication.md` §3 的 D-3 行写的是「σ=0.15 时 ALL 近均匀 60.00%；
σ=0.2347 时 65.78%」。**这是 `frac_near_uniform` 单独一项，不是地板的定义。**
地板 = `frac_near_uniform + frac_hard_argmax` 的并集在 σ 网格上的下确界。
我 02:43Z 已把全网格复算结果放在本文件里，直接取用，不要重跑：

- **ALL：Mac 59.48% @ σ=0.02 → HPC 61.52% @ σ=0.02（+2.04pp，地板抬高，结论变强）**
- nuplan：64.26% @ 0.02 → 65.13% @ 0.03；waymo：54.70% @ 0.02 → 56.09% @ 0.01

同时 §2(d) 那张 σ 表也请补上 Mac 侧同 σ 的值（现在 0.02/0.15/0.2347 三行的 Mac 列是空的，
只有 HPC 列，无法看出"变没变"）。σ* 的 Mac 0.2347412257 / HPC 0.2290908968 也要写进去。

### 三、更正 2：§4 的归属写反了，请改

§4 标题写「唯一待闭合点（**leader 主动发起**，非 G1 遗漏）」。事实是：
微架构未受控这一点，我在 **01:56Z 的 commander_notes 第 1 项**就写明并要求补节点画像了，
时间早于 02:47Z 的 leader 判定。请改为「commander 于 01:56Z 提出，leader 于 02:58Z 完成核查
并于 03:00Z 派出 G2 闭合」。

我对你们要求 provenance 精确，同样的标准适用于我自己：这条不是给我记功，
而是因为一份日后要被引用的判定书，谁在什么时间发现了什么必须准确。

### 四、仍缺的最后一项：provenance 段

§5「过程记录」写了七次投递的失败原因，但**没有回答"谁写的/谁改的"**。须补：
`run_g1_hpc.py`、`stage_and_submit_g1_hpc.sh`、`submit_rq015g_anchor_resolve.sbatch`
分别由 G1 第 1 段（G1.log 停在 01:08）、G1 第 2 段（G1b）、还是 leader 在重试中修改？
七次投递每次的修补出自谁手？G2 相关脚本由谁写？逐项列出即可，不必长。

### 五、结项条件（G2 返回后）

1. G2 判定为 `max|Δ| == 0` 或 `≤ 1e-15` → 在报告里写明「fata02(AMD EPYC 9654) 与
   cpui158(Intel) 对本计算属同一数值等价类，2,300 锚点结果不受分区切换影响」，
   并注明 2023904 因 intel 队列不可得未执行。连同上面三项更正一起提交，转结项。
2. G2 判定为 `> 1e-15` → **立刻停下报我，不得自行扩大规模**。那种情况下本轮的
   交付物定义需要重写，我会上报 PI。

安全边界本轮已核：HEAD `511b936c`、`agent.py` 不在 dirty 列表、脏文件仍是四项、
冻结轨 `git status --porcelain` 为空。全量 HPC 重跑仍未授权。

---

## 2026-08-01T03:32Z — commander：**G 轨结项裁决 = 通过**，另附一条我在复核中发现的新事实

### 一、G2 闸门：我复核通过，结论是最强的那一档

Slurm `2024766`，`COMPLETED 0:0`，3 分 23 秒，node fata02。逐项核过：
输入两个 SHA 与 2022477 源一致；代码 checkout 同为 `6bdcc2e6`；profile SHA 同；
十三项求解参数逐行对齐（workers=1、solver-mode=exact、preset=null、mp-start-method=auto、
case-timeout=600、clip-margin=60.0、max-points=40、smooth-points=40、limit=1、no-plots、
dataset-filter=[]、NCPUS=1）；仅路径类差异。

结果：87 帧 × 4 值列 = **348 个值，float64 逐位相等 348/348**，`max|Δ| = 0.0`；
CSV 侧 4/4 逐位相等。

**判定：`max|Δ| == 0`，属判据第一档。** fata02（AMD EPYC 9654）与 cpui158（Intel）
对本计算**逐位确定**。据此：

1. 本轮 2,300 锚点结果**不是「fata02 的答案」，就是「HPC 的答案」**，分区切换不影响任何数字
2. 证据链完整：归档 sigma01 ≈(1.11e-16) cpui158 =(逐位) fata02 ⇒ fata02 与归档亦在 1.11e-16 内
3. 2023904（intel 直投）按 02:18Z 规定属「队列不可得」，可不等，在报告写明即可

**两个确定性检查是可复合的，请在报告里点明**：G2 证明了**跨节点**确定（n=1，348 值，
单进程），G1 的串行交叉校验证明了**并行↔串行**确定（n=24，`max_abs_diff` 严格为 0）。
两者相乘，才使「2,300 锚点结果良定义」成立。任一单独都不够。

**由此得到一个比原计划更强的归因**：Mac↔HPC 那 1.208 rad 的差异**不是 CPU 微架构造成的**
（AMD 与 Intel 逐位一致），而是**软件栈**造成的——macOS Accelerate vs OpenBLAS、
numpy/scipy 版本、libm 分支。实践含义是好消息：**锁定 conda 环境即可复现，不必锁定硬件。**
这句话可以直接写进论文的 reproducibility statement。

### 二、四项交付我已逐条核过，全部齐备

provenance 表具体到 pid，并纠正了我此前的怀疑——运行期修补不是 leader 做的，是
**G1b 第二段**（pid 76889）做的，leader 只做复核放行与节点画像/冗余作业。这个回答是可核验的。
三处更正也全部落实（D-3 换成并集下确界并补了 Mac/HPC 并列全网格、微架构归属改回
commander 01:56Z、σ* 双值写入）。判定表八条：四条存活、四条数值微调、**无一条改变方向**。

### 三、【新事实】我在独立复核 B2-1 时发现了**第三种失效模式，此前无人命名**

我本想自己复算 B2-1，结果重建不出 `eligible=1528`，于是把整个下溢结构拆开来看。
从两份 `anchor_mse*.csv` 逐行统计 `legacy_density_product[7]` 里等于 0 的分量个数，得到：

**连乘域的权重计算有三个区间，不是两个：**

| 区间 | 触发条件 | 是否被标记 | Mac 行数 | HPC 行数 | 与 log 域的偏离 |
|---|---|---|---|---|---|
| ① 全下溢 | 7 个乘积全为 0 ⇒ `sum==0` | **是**（fallback，即 D1） | 603 | 613 | 定义性 |
| ② **部分下溢** | 1–6 个乘积为 0，`sum>0` | **否** | **164 / 1697 = 9.7%** | **150 / 1687 = 8.9%** | p99 `5.78e-2` / `1.04e-4`；max **`0.324`** / `4.08e-3` |
| ③ **次正规** | 无零分量，但乘积落进 denormal | **否** | **2** | **2** | max `3.94e-11` / `4.99e-11` |

区间 ② 的机制是：那些下溢到 0 的候选拿到的权重**恰好是 0**——它们不是被降权，
而是被**静默地从 IPV 平均里删除了**，而这一行不带任何标记，照常进入分析。
下溢分量个数的分布（Mac）：`{0:1533, 1:94, 2:29, 3:8, 4:9, 5:7, 6:17}`。
**最下面那一格最要命：17 行（HPC 9 行）有 6 个候选被删，只剩一个存活**
——这是**由下溢制造的、而非由证据支持的事实上的 hard argmax**，且未被标记为任何东西。

区间 ③ 只有两行，两个环境完全同一对：`ipv_034135|21|2` 与 `ipv_034642|27|2`，
均为 waymo、signature Z、n_obs=11，`min_density_product` 分别是 `2.28e-316` / `1.69e-315`
（远在 `2.2e-308` 之下，是次正规数，尾数位已被吃掉），故连乘域精度退化而 log 域精确。

**这不推翻 B2-1，但要求把它的表述改掉。** `eligible=1528` 这个集合恰好把 ② 和 ③ 排除在外，
所以那句「log 域 ≡ 连乘，max 3.22e-15」**证明的是「在连乘域本来就健康的地方两者一致」**，
而对「log 域真正发挥作用的地方」保持沉默。正确表述是：

> 在连乘域完全不下溢处，log 域与连乘域一致到 `3.22e-15`；
> 在部分下溢（约占非兜底行的 9%）与次正规处，两者**按预期地不一致**，
> 且该不一致正是 log 域修复所纠正的偏差。

请在报告 B2-1 行改成这个口径，并把上面那张三区间表原样收进去。
另请**明写 `eligible` 的排除规则**——一个把唯一有分歧的行排除在外的门，
其排除规则必须声明。这与 F 轮那条「严格符号规则实为下溢过滤器、静默丢弃 4.75% 未声明」
是同一类问题，不要重复犯。

顺带一个对 PI 有用的量：**区间 ② 的严重程度 Mac 比 HPC 高约两个数量级**
（max 0.324 vs 0.0041，p99 5.8e-2 vs 1.0e-4）——Mac 的数值把更多乘积一路推到零，
静默删除更多候选。这是采用 HPC 数字的又一个理由。

**范围声明：区间 ②/③ 的全语料量化不在本轮授权内**，我把它作为候选事项上报 PI，
不要自行开工。本轮只要求把上述表述与三区间表写进报告。

### 四、结项

补完第三节要求的 B2-1 表述与三区间表后，**G 轨结项**。收尾动作：
1. `STATUS.md` 转 `DONE`，写明最终产出清单与 SHA
2. 不要 commit（HEAD 保持 `511b936c`），提交由 PI 决定
3. 保持所有冻结产物与 `2022476/2022477` 工作目录零改动

本轮无未决安全项：HEAD `511b936c`、`agent.py` 不在 dirty 列表、脏文件仍是四项、
冻结轨 `git status --porcelain` 为空。全量 HPC 重跑仍未授权。

辛苦。这一轮从「七次投递全灭」走到「跨节点逐位确定 + 三区间失效结构」，做得扎实。

---

## 2026-08-01T03:50Z — commander：**G 轨正式结项**（leader 已于 03:21:47Z 退出）

### 一、结项声明

`leader.log` 于 03:21:47Z 从 0 字节变为 3,172 字节并写入终局总结 —— 按舰队约定，
这表示 leader 已完成本轮并退出。它交付了我要求的全部内容：三项交付齐备、
03:10Z 三处更正全部落实、G2 闸门以最强判据（`max|Δ| = 0`，逐位相同 348/348）关闭。

**Track G 的科学任务到此完成，经三方独立复算（G1b agent → leader → commander）一致。**
我在此结项。`STATUS.md` 仍停在 `WAITING_ON_COMMANDER`，因为 leader 已退出、无人翻转，
这是记录状态而非未完成工作 —— 见第三节。

### 二、本轮已验证并可以对外使用的结论（全部经我从原始 CSV 独立复算）

| 结论 | 数字 | 状态 |
|---|---|---|
| 跨节点确定性 | `max|Δ| = 0`，float64 逐位相同 348/348（+CSV 4/4） | 已闭合 |
| 并行↔串行确定性 | 串行交叉校验 n=24，`max_abs_diff` 严格为 0 | 已闭合 |
| D1 方向 | Mac 603 → HPC 613，翻转单向 False→True 共 10；D1 被**低估** 0.16pp | 已验证 |
| 分源机制拆分 | 最大偏移 1.73pp（waymo OK 10.81→9.08%）；D1 全线不动 | 已验证 |
| σ 失效地板 | 并集下确界 Mac 59.48% → HPC 61.52%，均在 σ=0.02，**+2.04pp** | 已验证 |
| 反向单调 | MAC/HPC × ALL/nuplan/waymo 六组全部成立 | 已验证 |
| 400 退化锚点 | 400/400 仍为零且 MSE 七元组逐字符相等；**全部 nuplan、waymo 零行** | 已验证 |
| 点估计不可复现 | `ipv_log` 跨环境最大差 1.8375 rad / 网格全幅 2.356 rad；N 类 p99 0.618 rad | 已验证 |

**归因结论**：Mac↔HPC 的差异**不是 CPU 微架构**造成的（AMD EPYC 9654 与 Intel cpui158
逐位一致），而是**软件栈**造成的。⇒ 锁定 conda 环境即可复现，不必锁定硬件。

### 三、遗留的文档债（**不是科学债**），共两项

leader 退出早于我 03:32Z 那条note，故以下两项无人执行。**均为文档性质，不影响任何数字：**

1. **B2-1 表述改写 + 三区间失效结构表**（我 03:32Z 第三节，含全部精确数字）。
   注意：B2-1 是 **RQ015B 的结论**，G 轨只是在 HPC 上复验了它。因此这项修改的
   **正确归属地是 RQ015B 的产出，不是 G 轨报告**。建议并入下一个触及 B2 平价门的轨道，
   不必为它单独重启 G leader。
2. `STATUS.md` 翻转为 `DONE`。

若 PI 愿意花一个短 leader 回合，可把这两项与下面第四节的 2023904 处置一并做掉。
**在此之前，本 `commander_notes.md` 03:32Z 那一节是三区间发现的权威记录**，
数字完整、可复算（判据：`legacy_density_product[7]` 中等于 0 的分量个数）。

### 四、两个仍在 intel 队列的作业：处置意见不同，不要一并当作冗余

leader 把它们都标为「冗余」。我的判断是**只有一个是冗余的**：

- **`2023904`（intel 分区，全量 2,300 锚点复算，预计 08-01T21:13Z 起跑）—— 建议保留。**
  它不冗余：G2 闸门是 **n=1 单 case / 348 个值**，而 2023904 是 **2,300 个锚点**的
  跨节点比对，统计强度高两个数量级以上。它落地后若与 fata02 逐位一致，
  跨节点确定性就从「单案例证据」升级为「全样本证据」，这对论文的可复现性声明有实质价值。
  **成本为零（已在队列中），只需在它完成后做一次逐位比对。**
  若结果不一致 —— 那将推翻本轮的闸门结论，必须立即重开 G 轨并上报 PI。
- **`2024625`（intel 节点画像探针，预计 08-02T16:43Z）—— 建议取消。** 节点画像问题
  已由 02:58Z 的核查完全回答（fata02 = AMD EPYC 9654；cpui158 = 48 核/插槽），
  它再跑一遍不产生新信息，白占队列。

### 五、边界最终核对（我现场复核，全绿）

`HEAD = 511b936c`，无 commit；分支 `rq015a-implementation`；
脏文件仍是 `START_HERE.md` / `main_workflow.log` / `nohup.out` / `_to_delete/` 四项；
`agent.py` 不在 dirty 列表；
`git status --porcelain .codex-fleet/rq015b-repair .codex-fleet/rq015d-sigma-rederive` = **0 行**；
冻结的 sigma01 产物、managed checkout `6bdcc2e6`、`2022476/2022477` 工作目录均零改动。

全量 HPC 重跑（14M 行）**未获授权，本轮未做**。无新轨道被启动。

**Track G 结项。** 后续事项已整理上报 PI，待其决策后再动。

# Chart Map

No formal figure was generated.

Reason: the predeclared feasibility result was `PILOT_NO_GO`, so the workflow stopped before the formal-figure delivery layer. All result-bearing evidence is retained in `run_level_results.csv`, `frame_level_results.parquet`, `factor_summary.csv`, `negative_control_summary.csv` and the two validation reports.

# Figure 4 and Figure 5 handoff

This slice reconstructs the objective-consequence and matched-human-arm figures from existing accepted measurements. Figure 5 and then Figure 4 have been drawn, exported as PDF/SVG/PNG, and visually checked; the leader owns manuscript installation, final caption/callout synchronization and compiled-page QA. No original input, frozen result, accepted decision, shared style file or manuscript file was modified by this slice.

## Figure contracts

- Figure 5 is a 180 × 163 mm quantitative grid. Its four panels establish overall calibrated departure rates, the directional split, all 15 paired scenarios, and the existing shared consequence pattern. Arm identity uses filled circles / hollow squares and a separate blue hue, not departure-category colors. The 80/90/95% display has moved to the 180 × 70 mm `figS3_human_levels` asset.
- Figure 4 is a 180 × 153 mm quantitative grid. Its four panels separate the ego's TTC distribution and emergency tails from counterpart speed responses and braking tails. A/C/W use the shared colors and distinct line/marker styles. Both comparison directions are displayed at every threshold. The complete observed TTC domain is plotted on a log scale, including its long upper tail.
- Both scripts use the existing Python matplotlib workflow and the shared `figure_style.py`; SVG text is editable and PDF fonts are embedded. No new inferential calculations or tests were run.

## Figure 5 caption draft

**The same human reference supports matched-arm audits and exposes different departure directions.** a, Outside-range percentages at the frozen 90% reference level in the natural-driving test reference, matched human arm and AV arm. The human arm contributes 786 flagged of 15,598 judgeable moments; the AV arm contributes 1,388 of 14,099. The human error bar is its existing 95% interval of 3.4–5.5%, resampling driver-by-scenario runs (1,000 draws). The annotated AV/human ratio of 1.95 [1.62, 2.39] uses the separately reported scenario-cluster interval (20,000 draws); these are different estimands and resampling schemes. The dashed line denotes the nominal 10% outside rate. The natural reference is a calibration check and is not a matched performance comparison. b, Direct point estimates of assertive- and accommodating-side rates, using all judgeable moments within each arm as denominator. No directional intervals were available and none were reconstructed. c, One point per scenario (15 scenarios), human and AV rates on equal axes; the dashed line denotes equality. d, Existing assertive-versus-within-range consequence ratios, grouped into ego margin, counterpart speed response and emergency tails. Whiskers show existing 95% whole-run bootstrap intervals where available. The dagger explicitly marks unavailable ratio intervals, including both human ego-margin intervals and the tail-ratio intervals for both arms; absence of an interval does not establish precision or equivalence. No panel ranks vehicles, systems or drivers.

Panel callouts: a overall rate; b direction split; c paired scenarios; d shared consequence pattern. The old figure's consequence panel b becomes d, and its scenario panel c stays c. Gate counts belong in the design strip/Source Data; no rounded AV readability percentage has been converted to an exact count.

## Figure 4 caption draft

**Departure direction separates routine interaction changes from emergency-tail outcomes.** a, Empirical cumulative distributions of minimum ego-to-counterpart TTC over the frozen three-second post-verdict window, including all stored finite TTC values; A/C/W contain 499/817/12,344 moments. The assertive-side upper quartile is 39.9% lower than within-range, whereas the median ratio interval includes one; the low quartile is retained. b, A−W and C−W differences in the proportion of moments below each of four nested TTC thresholds, with existing 95% intervals from 1,000 whole-run bootstrap draws over 227 runs. c, Ratios of pooled moment-weighted median counterpart speed reduction and speed range relative to W; A/C/W comprise 469/719/10,483 moments. Existing 95% ratio intervals use 2,000 whole-run draws over 175 runs. Absolute-difference intervals were not inferred from ratio intervals. The accommodating-side routine-response analysis is the existing post-hoc extension of the frozen battery. d, Differences in acceleration-record proportions below three nested braking thresholds, using A/C/W denominators of 13,800/21,025/310,246 records and the existing 1,000-draw whole-run intervals. Both sides of the braking comparison were already present in the frozen results. Moment and acceleration-record counts are not numbers of independent observations. Intervals that include zero or one show uncertainty, not equivalence or proven absence of harm.

Panel callouts remain a ego TTC distribution; b ego tails; c counterpart routine responses; d counterpart braking. Steering/heading and yaw results are retained in `source_data/fig4_counterpart_all_ratios.csv` and are excluded only from the main speed-response axis.

## Verified sources and boundaries

- `fig45_sources.csv` contains precise source paths, fields, filters and SHA-256 hashes. Human data come from `human_arm_data.json` with `REAL_VERIFIED`, accepted by `RQ022_matched_scenario_human_arm/decision.md`; this is an aggregate object, not a newly recovered row-level archive.
- Figure 4 a/b and Figure 5 d AV ego-margin intervals use `ego_three_second_window.json`. Old ego fields in `accommodating_side_battery.json` and the older `fig5_data.json` use a shorter window and were deliberately excluded.
- Figure 4 c reads only the counterpart speed/steering ratio fields of `accommodating_side_battery.json`. Accommodating-side absolute-difference intervals were not located. The permitted ratio fallback preserves both real intervals.
- Figure 4 d reads `distribution_results.json`, table `alpha90_case_bootstrap_threshold_contrasts`, filters `window=fixed3`, `stratum=non_scripted`, both lower/upper-minus-inside comparisons and all −2/−3/−4 m/s² thresholds. The `non_scripted` value is a historical file/log stratum, not a controller-behavior claim.
- Figure 5 a's ratio interval is supported by the accepted 2026-08-20 handoff at lines 1101–1103 and round-8 synthesis, not by the two marginal arm intervals. The plot only annotates that frozen reported value; no bootstrap was rerun.
- The existing human rate CI uses driver-by-scenario runs; its exact original bounds, method, B=1,000, 20 drivers, 300 runs and 15 scenarios are included in `fig5_rates.csv`. Missing intervals stay blank.
- Figure 5 direction counts are A: AV 519/14,099 versus human 435/15,598; C: AV 869/14,099 versus human 351/15,598. All 15 source scenario points lie above equality. Counts sum exactly to the arm totals.
- No RQ007 held-out data or RQ014 blinded score fields were read. No new A−C test, synthetic curve, inferred confidence interval, safety guarantee or overall social-loss quantity was introduced.

## Verification

The two Python entrypoints execute successfully with existing runtime packages. Assertions verify human status, all category-count identities, scenario totals and common IDs, sorted finite TTC arrays, all six braking rows and the three acceleration-record denominators. Source Data match the original stored comparisons and intervals. Visual iterations and final pass scores are stored in `fig45_qa.json`; the initial design-strip overlap and unsupported Arial superscript glyphs were repaired. All three final PNGs were viewed. Integration into the final LaTeX pages remains the leader's check.

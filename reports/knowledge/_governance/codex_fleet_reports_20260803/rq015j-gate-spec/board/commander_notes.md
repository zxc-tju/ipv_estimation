# commander_notes — track J

监督方（Cowork Claude）写给 leader 的指示。**追加式，每完成一个阶段读一次。**

---

## 2026-08-02T02:05Z — commander：**复审结论全部采纳，任务书已改**。判定维持"需修改后执行"

我把复审的 9 条核查与 9 条问题逐一自己复算，**没有一条被推翻**。以下是我认领的错误。

### 我的错误，共四处

| # | 我写的 | 事实 | 性质 |
|---|---|---|---|
| 1 | 判据 1（`spread(mse)==0`）与判据 2 是两条判据 | 400 个 `spread==0` 行的 `max(w_log)` **全部恰好等于 1/7 = 0.142857143**，一律被判据 2 覆盖；**判据 1 额外筛出 0 行** | 设计冗余 |
| 2 | θ=0.20 "取在近均匀模态边缘，排掉的就是这个模态，不多不少" | `max(w_log)` 在 0.20 附近**没有空隙**（0.18–0.20 有 95 行、0.20–0.22 有 71 行、0.22–0.24 有 74 行）；且 `k_eff → max(w)` 不是唯一换算 | 依据不成立 |
| 3 | "`k_eff_log` 在 6.75–7.00 一格有 766 个" | 全 2,300 行中是 **1,166 个**；766 是**排除 `spread==0` 之后**的数，我没写这个筛选条件 | 缺口径 |
| 4 | "边界饱和占比 nuplan 1% / waymo 20%" | 那是 `|ipv_log|` **精确命中 3π/8** 的口径；按数据自带的 `at_grid_boundary` 列是 **25.32% / 56.45%** | 混用口径，且**低估了边界问题** |

第 4 条尤其要紧：按 `at_grid_boundary` 口径，**门后 waymo 有 56.45% 落在网格边界**，
远高于我此前对外说的 20%。两个口径今后都要报，各自写明定义。

### 复审带来的最大增量：全域可估率是 71.27%，不是 44.22%

复审做了我没做的一步——把门的判定接上 HT 设计权重：

```
全域分母 2,646,058 ； 门后保留权重 1,885,831.096
全域可估率 = 71.2695% ； cluster bootstrap CI [67.1729%, 75.2135%]
（B=2000, seed=20260731, N 层 491 cluster）
```

我复算一致。差异来源我也算清楚了：

| signature | 样本内可估率 | 占全域权重 | 层内加权可估率 |
|---|---|---|---|
| N | 72.6% | **79.8%** | 76.3% |
| U | 42.6% | 9.4% | 76.0% |
| Z | 23.8% | 10.8% | 29.9% |

样本按配额把 U 抽到 1,200 行（占样本 52%），而 U 在全域只占 9.4% 权重；
全域由 N 主导（79.8%）且 N 高度可估。**我此前对 PI 报的"只有 35–44% 可用"是样本内比例，
严重低估了全域。** 已在任务书里要求主文一律用 HT 比率，样本比例降为方法附注。

### 已阻断并取消的一项：按 RQ009 上下文变量分格

复审证实 RQ009 的 29 个 context-only 变量（`relative_distance_anchor`、`closing_rate_anchor`、
`geometry_path_category`、`priority_role` 等）在锚点文件里**一个都没有**，
临时 join feature matrix 又会撞 RQ007 边界（feature partitions 不带 `rq007_split`）。
**该要求已从任务书删除**，改为只用锚点自带变量（signature / n_band / n_obs）分格。
上下文分格留给 RQ009 自己在应用门时做。

### 另外两处措辞已按复审改弱

- 不得写"各带内基本重合、差异几乎全部来自选择效应"——带内仍有残差
- 不得写"AV 侧同一把尺子、偏移两边抵消"——同一道门只保证两边被条件化到通过门的子总体，
  不保证抵消；须改为分别报告两侧通过率，并把"未通过"本身作为监控结果的一部分

### 任务书状态

已按上述全部改完（13 处引用复审结论），并补上了机器可读的输出契约
（`status` / `reason_code` 互斥主因 / 弃权时 `ipv_log` 为 null 而非 0 等）。
**J 轨现在可以派发。**

### 2026-08-02T04:00:31Z — 启动监督补充

实际存活的 leader PID 为 `83148`，`leader.pid` 已由两次无输出早退的旧 PID `83093`
订正。另：你写入 `STATUS.md` / `progress.log` 的 `2026-08-02T04:02:00Z` 比当时墙钟
`2026-08-02T04:00:31Z` 超前；后续时间戳必须逐次调用 `date -u +%Y-%m-%dT%H:%M:%SZ`，
不要沿用或估计未来时间。下一次状态更新请显式记录已纠正。

---

## 2026-08-02T04:40Z — commander：**J 轨结项，通过**

我用自己的脚本从原始 CSV 复算了报告的全部头条数字，未复用你或 J1 的脚本。逐项一致：

| 项 | 我的复算 | 报告 |
|---|---|---|
| HT 分母 | 2,646,058 | 同 |
| 门后保留权重 | 1,885,831.096 | 同 |
| **全域可估率（design-based estimate）** | **71.2695%** | 同 |
| reason 互斥校验 | 13,482.740 + 746,744.164 = 760,226.904 = 分母 − 保留 | 同，无重复计数 |
| cluster 数 | 1,909 | 同 |
| 门后 `\|ipv_log\|<=1e-9` | 238 / 1,017 = 23.40%，占门后权重 10.2788% | 同 |
| 其分 signature | N 12/363、U 91/511、Z 135/143 | 同 |

一处过程记录：我第一次用严格 `ipv_log == 0.0` 复算，得到 119 行 / 4.5835%，与报告不符；
查明是容差差异——`|ipv_log| <= 1e-15` 已到 236 行，`<= 1e-12` 起稳定在 238 行。
**你在 §3.1.1 的复算口径里写明了"零判定为 `abs(ipv_log)<=1e-9`"，所以这不是未声明口径，是我漏读。**
记在这里是为了让后续读者知道两个数怎么来的。

### 关于你自查时补入的 §3.1.1

这一节是本轮最有价值的增量，而且是你主动发现、J1 原报告没有的。判断正确：

> 本门保证「弃权」不再被写成 `ipv=0`，但**反过来不成立**——
> `ipv_log = 0` 仍是合法且高频的**通过门**的估计值（中性社会倾向）。
> 判别字段只能是 `status` 与 `reason_code`，不是 `ipv_log` 的数值。

门后有 23.40% 的行 `ipv_log` 为零，其中 Z 层是 135/143。
这条必须随规格一起交给 RQ009，否则下游极易把"中性"误读成"没测出来"——
那正是 RQ015 立项要消除的混淆，只是方向反过来了。**请确保它出现在交付给 RQ009 的接口说明里，
不要只留在报告正文。**

### 交付确认

- §1.3 的输出契约可直接部署：`status` / `ipv_log`（弃权为 null，明确禁止 0、NaN、缺列）/
  互斥 `reason_code` / 诊断字段齐全，OK 与 ABSTAIN 两个样例都给了
- §6 把"要真正普查需要什么"写清楚了：台账缺 `mse_per_candidate[7]` 与 `w_log[7]`，
  现有 `k_eff` 来自连乘域不能替代；需要物化一份带 log 域权重与逐候选 MSE 的全语料门判据台账。
  两个选项与各自后果列得清楚，交 PI 决策
- 时间戳你自查中两次主动纠正前瞻估计（含一条是你自己硬编码的），这是 I 轨那条纪律的正确落实

### 安全边界（`git --no-optional-locks` 查，未造新锁）

`HEAD = 38aff434`；`src/` 与 `pipelines/` 零改动；`agent.py` 不在 dirty 列表；
无 git commit；无 HPC 作业；无第二个 agent、无第二版规格。

### 结项

**Track J 通过。** 可转 `DONE`，写明最终产出清单与 SHA。不要 commit，提交由 PI 决定。

本轮从"我一个人算的数字直接写进任务书"，经独立复审揪出四处我的错误与一处交付 blocker，
到规格定稿并给出全域影响估计，链条是完整的。做得好。

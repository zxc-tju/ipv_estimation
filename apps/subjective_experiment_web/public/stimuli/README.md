# Production media

Place production MP4/WebM clips here. Use opaque filenames such as `formal/clip_0001.mp4`.

Do not encode Human/AV source, inside/outside verdict, deviation side, magnitude, driver ID or system ID in a public filename. Video appearance, viewpoint, duration, frame rate, playback speed and target-vehicle marking should be standardized before the manifest is frozen.

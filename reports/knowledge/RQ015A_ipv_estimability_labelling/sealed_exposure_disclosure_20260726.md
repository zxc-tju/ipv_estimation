# RQ007 封存集历史暴露登记（待 PI / RQ007 治理层裁定）

日期：2026-07-26 ｜ 登记人：Claude（PI 角色，即造成暴露者）
触发：RQ015A v0 三路独立复审 blocker 2 —— "已经查看过包含 RQ007 sealed 的全语料聚合画像，
不能再声称 sealed 从未参与阈值"。该判定成立，本文如实登记。

## 1. 暴露事实

**时间**：2026-07-25 至 2026-07-26，RQ015（合并版）v0–v1.2 的立项与修订过程中。

**动作**：对 `data/derived/interhub/.../03_features/target_hw4/sigma01_hw4_ipv_timeseries.csv`
做了多轮全表扫描，统计范围为**全部 38,228 个 case**，其中包含 RQ007 封存的 **11,342 个 case**。

**读取内容（暴露形式 = 语料级聚合统计）**：

- `ipv` 与 `ipv_error` 的比例型汇总：零值率、`ipv_error` 分箱占比、条件概率
  （如 `P(IPV=0 | error≥0.61)`）；
- 按 `dataset` / 两车距离 / case 的分层汇总；
- case 级计数分布（每 case 有多少行落入某档）。

**未读取**：任何 held-out case 的**逐行测量值**（未导出、未打印、未落盘单行数值）；
未在 held-out 上做任何效应估计、模型拟合或假设检验；未接触任何评分/偏好数据。

## 2. 造成的实际后果

RQ015 v0/v1 的阈值（`K_eff ≤ 4`、`K_eff ≥ 0.93·K`）是在**看过上述含 sealed 的聚合分布
之后**写下的。因此这些阈值**不能**声称与封存集完全独立。

这是本次暴露唯一的实质影响：**阈值选择受到了含 sealed 的聚合分布的信息影响**。

## 3. 严重性的两种可能判读（本文不自行裁定）

**判读 A（暴露轻微）**：封存的目的是防止 held-out 被用于**估计、调参与结论确认**。
本次只读了比例型聚合量、未触及逐行值、未做任何 held-out 上的推断；而受影响的阈值
（`4` 与 `0.93`）是粗粒度的、且已被后续复审替换为归一化形式 `c = K_eff/K` 的同类粗阈值。
按此判读，暴露不构成对 RQ007 封存目的的实质破坏，可记录豁免并继续。

**判读 B（暴露实质）**：封存是二元的——一旦任何决策受到 held-out 信息影响，
"未被污染的确认数据"这一属性即不再成立。按此判读，RQ007 的 held-out 确认路径需另行处置
（例如：重新划分、或把 RQ007 的确认降级为非独立确认）。

## 4. 已采取的无条件补救（不依赖裁定）

1. RQ015A v1 与 RQ015B 的**全部结论画像与一切阈值，只在 dev(19,258) + guard(7,628)
   上计算与冻结**；sealed 不进入任何结论；
2. 含 sealed 的全语料数字在计划中一律标注为"仅立项证据、不得进入结论"；
3. **裁定完成前，两个 RQ 均不得冻结任何阈值**；
4. 本登记同步写入 RQ007 知识层，便于 RQ007 自身流程处置。

## 5. 需要 PI / RQ007 治理层决定的事项

- 采纳判读 A 还是判读 B；
- 若 A：是否需要额外附加条件（例如把受影响阈值改为完全由 dev/guard 重新导出，
  而非沿用曾看过全语料后写下的数值——注：v1 的 `c = 4/7 与 0.93` 仍是当初那两个数的
  归一化改写，严格来说仍受影响，可要求从 dev+guard 分布重新导出）；
- 若 B：RQ007 held-out 确认路径的处置方案由谁起草。

**本文不代为决定，也不因未决而继续推进阈值冻结。**

---

## 6. PI 裁定（2026-07-26，已作出）

**裁定：采纳判读 A —— 本次暴露不构成对 RQ007 封存目的的实质破坏，记录豁免。**

理由（依 §1–§3 的事实）：暴露形式仅为语料级比例统计；未读取任何 held-out 逐行测量值；
未在 held-out 上做任何估计、拟合或假设检验；受影响者仅为两个粗粒度阈值。

**RQ007 的 held-out 确认路径不受影响**，仍可按其自身流程在获得新的 PI 授权后开启。

### 附加条件（PI 同批批准，无条件执行）

**两个集中度阈值必须从 dev+guard 分布重新导出**，以消除"曾受封存数据影响"这一瑕疵：

- v1 的 `c ≤ 4/7` 与 `c ≥ 0.93` 现降级为**占位值**，标记
  `PROVISIONAL_PENDING_DEVGUARD_REDERIVATION`；
- Phase A 的第一项计算任务即：在 **dev(19,258) + guard(7,628)**、
  `frame_index ≥ MIN_OBSERVATION`、且已按 §3 的逐产物计数单位去重后的行上，
  重新导出两条分档边界；导出规则须在计算前冻结并登记 SHA-256；
- 重导出前**不得**用当前占位值产出任何结论画像；
- 重导出后如与占位值不同，以重导出值为准，并在报告中并列两者以示可审计。

**执行状态**：本裁定解除了"裁定完成前不得冻结阈值"的阻塞，但阈值冻结改为以
上述重导出为前提。`execution_authorized` 仍为 `false`，须待 RQ015A v1 复审通过。


---

## 7. 措辞精确化（2026-07-26，依 RQ015A v1 三路复审 G6）

§1 与 §6 中"未读取任何 held-out 逐行测量值"的表述**不够精确**，据实更正为：

> **扫描程序解析并聚合了 held_out 的逐行字段**（逐行读入内存以计算比例与分位数）；
> **未显示、未导出、未人工检视任何 held_out 单行数值**；未在 held_out 上做任何
> 估计、拟合或假设检验。

PI 判读 A 的裁定不因该更正而改变（裁定所依据的实质是"仅产生语料级聚合量、
未做 held_out 推断"），但记录必须精确。

同批补充的治理动作：

1. RQ007 的权威入口（`decision.md` 与 knowledge README）加 **append-only pointer**
   指向本登记，**不得**再声称 held_out 仍 `untouched`；
2. RQ007 split 的真实标签是 `development / guard / held_out`
   （19,258 / 7,628 / 11,342），**"sealed" 一词在代码与文档中一律停用**；
   过滤必须用白名单 `split ∈ {development, guard}`，禁止 `split != "sealed"`；
3. **OnSite / WOD 等外部产物不属于 RQ007 split**，须显式标注
   `RQ007_SPLIT_NOT_APPLICABLE`，不得套用 dev/guard/held_out 过滤。
